# Engineer lifecycle — build plan, SLICE 2 of 2 (rev10)

> **Split (rev10, 19 Sep 2026).** This document is **slice 2 of two**, **split out of the add-engineer plan at `eb8a9c2` after external review**, by content only. Slice 1 is `docs/plans/add-engineer-plan.md`. **The episodes design (D15) reverses D10 and has NOT been externally reviewed in this shape:** external review signed off on the five-column attribution version; the episodes table, its grants, its lifecycle, the board's read of it and the lifecycle functions are new since that sign-off and **need the full review package** (§6).
>
> **What this document contains.** **Deactivate** (§2.9) and **reactivate** (§2.10); the **`engineer_episodes` table**, its index and RLS (§2.8); the **D13 dated board rule** (§4.9); the **half-status / evening-chip change (Edge 1)** (§4.9); the classifier's **`deactivated_on_this_project` outcome (Edge 2)** (§2.10, §4 R5b); the **unreachability report** (§2.11); findings F1–F5; decisions D10 (reversed), D13–D15, D17–D20; tests T10b, T11, T16 (extended), T23–T43. *(The deploy-order rule and the Preview-database finding are in slice 1 §6.1; slice 2 states only its own dependency, §4.9.)*
>
> **What it explicitly does NOT contain.** The add screen, paste-and-preview, the dry-run flag, the add function's original design and the shared authorisation helper, the `users` columns `registered_by`/`registered_at`/`consent_attested`, the six-role CHECK, the S1 lint, phone validation, the boundary literal, the add-screen strings, the D12 decision and rider, N1 — **all of that is slice 1.**
>
> **Dependency.** **Slice 2 depends on slice 1:** on its `users` columns (`registered_by`, `registered_at`, `consent_attested` — the person/number facts the episodes design keeps on `users`), on its **shared internal authorisation helper** (§2.7 of slice 1: caller resolved by `users.auth_id = auth.uid()`, tenant bound NULL-safely, the D6 gate) which `deactivate_engineer` and `reactivate_engineer` reuse unchanged, and on `add_engineers_to_project`. **Slice 2 must not merge or apply before slice 1.**
>
> ## Consequences of the split for slice 2 — recorded as consequences, NOT as new decisions (rev10)
> 1. **Slice 2 redefines a function slice 1 has already shipped.** Slice 1's `add_engineers_to_project` opens **no episode** (episodes do not exist yet); §2.8's "opens on add" therefore means slice 2 changes that live SECURITY DEFINER function's logic. Same signature → `CREATE OR REPLACE` preserves its grants (`CLAUDE.md` §0 rule); changing live function logic trips review gate (a).
> 2. **Engineers added by slice 1 are legacy-shaped: they have no episode.** They are covered by the lazy-materialisation rule (§2.8) from `project_members.created_at`. The "one intentional duplication" (`users.registered_at` = the first episode's `opened_at`, T10b) holds only for engineers added **after** slice 2.
> 3. **Slice 2 is its own migration.** Slice 1's is `048` (ASSUMED name); slice 2's number is whatever is free when it is drafted (**ASSUMED `049`**, to be reserved then, `CLAUDE.md` §0 reservations rule).
> 4. **The board behaves two ways** for engineers with and without episodes — accepted (D18), named in §4.9.

**Reading rule.** Moved passages say "this slice", "the slice" and "the plan"; they are verbatim from the single plan at `eb8a9c2`, where they meant the add-engineer work as a whole. Read each as the slice that holds the passage (the section index above). Section numbers, decision numbers (D1–D20), finding labels (F1–F5, N1, the S1 lint), test numbers (T1–T43) and UNKNOWN numbers are **unchanged** — nothing is renumbered.

**Evidence (rev10).** This pass moved text between documents; it ran no command against the repo's code and no database query, and added no new evidence. Every cited fact still rests on the logs named in the banner below (`add-engineer-plan-rev9.txt`; the read-only test-db probes in `add-engineer-plan-rev7.txt`). The rev10 log (`add-engineer-plan-rev10.txt`) holds the item-by-item mapping from `eb8a9c2`.

**Section index (which document holds what):**
| Section | Lives in |
|---|---|
| Banner, Scope, dated corrections, §0 findings | both (each keeps only its own items; shared items are in both) |
| §1 Authorisation; §2.1–§2.7 (add function, dry-run, helper); §2.8 (`registered_*` columns) | **slice 1** |
| §2.6 (`engineer_episodes` row); §2.8 (episodes table); §2.9 Deactivate + F1–F5; §2.10 Reactivate; §2.11 Unreachability report | **slice 2** |
| §3 Phone numbers and the validator; §4 R1–R8; §4.5–§4.8 (D12); §4.10 (S1 lint, D12 rider, N1) | **slice 1** |
| §4 R5b; §4.8 (D13 stub); §4.9 D13 board rule, Edge 1, Preview finding, tests T23–T43 | **slice 2** |
| §5 Flow; §6 Migration (slice 1 contents) and §6.1 Deploy order; §7.1 T1–T22, T39; §7.2; §7.3; §8 (slice 1); §9 (add strings) | **slice 1** |
| §6 (slice 2 contents); §7.1 T10b, T11, T16 (extended); §8 (episodes); §9 (lifecycle strings); §10 `pg_*` rows | **slice 2** |
| §10, §11, §12, Decisions, UNKNOWNS | split by item (each item's number and label is unchanged) |

> PLAN ONLY. rev1 `c86c5b6` (2026-09-18 23:06 IST), rev2 `e67e297` (2026-09-18 23:41 IST), rev3 `9b9187c` (2026-09-19 00:15 IST), rev4 `dba9cfc`, rev5 `fad98e3`, rev6 `eff12a0`, rev7 `f407ba1`, rev8 `1765d21` — each pinned
> (`git show <sha>:docs/plans/add-engineer-plan.md`). rev9 written 2026-09-19 (IST) on `feat/add-engineer-plan`, base `origin/main` @ `0744110` (re-fetched; unchanged).
> No application code, no migration file, no SQL intended to ship. Tier: FULL.
>
> **Evidence rule.** Every claim cites source printed in `~/Desktop/add-engineer-plan-rev9.txt` ("the log"; older logs are named where a claim rests on them). **Database claims (`probe <letter>`, `pg_1`–`pg_7`) cite the read-only test-db probes printed in `~/Desktop/add-engineer-plan-rev7.txt`. rev8 and rev9 ran no database query: every rev9 fact is in a repo file, and no Supabase CLI command was run.** Citations are `path:line`; database claims cite `probe <letter>` (rev7 adds probes `pg_1`–`pg_7`, named as such); throwaway scripts
> (not in the repo, not shipping) are printed with their output. Anything not printed is **ASSUMED** or under UNKNOWNS. **Correction discipline.** Retracted claims stay visible as ~~strikethrough~~.
>
> **Settled by Aravind, applied without re-raising (slice 2's items):** ~~D10 record who deactivated and when~~ (**reversed, rev9**); **D13 (rev7) — in this slice, dated, not a status filter (§4.9); rev8: reactivate ships in this slice (§2.10);** **D15 (rev9): episodes.** *(D1–D9, D11, D12 are in slice 1.)*
>
> ## D13 settled (rev7) — one-paragraph version
> Deactivation stops the **forward-looking expectation** (today's board card, the Today "Needs your attention" list and its count, every date from the deactivation day on) and **does not erase history** (every earlier date, and every log the engineer submitted, is unchanged). The rule is dated — keyed on **episodes** (`engineer_episodes`, §2.8) in IST — ~~rev7/rev8: on `users.deactivated_at`/`reactivated_at`~~ — and a plain `users.status = 'active'` filter is **wrong** for it (§4.9). Two places where the rule as given collides with "history stays" are resolved by a **flagged default, D14** — not silently.
> **Deploy-order hazard, new in rev7 (§4.9):** the board query will read the new `engineer_episodes` table; if the app **merges** (merge is the deploy, rev8) before migration 048 is applied, Daily Logs and Today both render their error state (probe pg_7).
>
> ## rev9 — D15 SETTLED by Aravind: episodes, not columns
> The board's question is an interval question; columns answer it only for the latest interval. **`engineer_episodes`** (§2.8) replaces `users.deactivated_by/at` and rev8's `reactivated_by/at`; `users` keeps `registered_by`, `registered_at`, `consent_attested`. **The attribution design changed after external review signed off on the five-column version** — the table re-enters the review gate. Cost of the board reading episodes: one extra parallel read, no join, no per-date query — **not a blocking finding** (§4.9). Also: Preview database **undeterminable from the repo** (dashboard checklist, §4.9); unreachability now keys on `whatsapp_sessions` (§2.11); F5 recorded as latent; **the plan must be split before the build package (§13)**.
>
> ## rev8 review conditions that live here
> Edge 1 day-D chip (§4.9) · **reactivate ships** as the third definer function, with the classifier fix (§2.10) · F5 struck and answered (§2.9) · unreachability report (§2.11) · deploy order (the dependency on `engineer_episodes` is stated in §4.9). **Two of the instruction's premises are corrected, not smoothed:** `generate-one-dpr.ts` does not reach `dispatch.ts:418` (F5), and "clear or keep the first record" cannot preserve "past dates stay unchanged" (D15). *(The S1 lint, the D12 rider and N1 are in slice 1.)*

## Scope

**In scope (slice 2):** a **deactivate** control (§2.9) and a **reactivate** control (§2.10); the **`engineer_episodes` table** (rev9, §2.8; ~~`deactivated_by`, `deactivated_at`~~ are not created); **Added in rev7 (D13, §4.9):** the dated deactivation rule in `lib/daily-logs/query.ts` (`getDailyLogsBoard`), the Today loop, a card marker, and a project-detail marker; the half-status change (Edge 1); the classifier outcome `deactivated_on_this_project` (Edge 2); the unreachability report (§2.11); D18–D20.
**Deferred, named:** **freeing a number** (deactivate does not, §2.9); ~~reactivating from the dashboard~~ (rev8: **ships in this slice**, §2.10); ~~the sweep and Daily Logs board not consulting `users.status` (F1, F4)~~ (rev7: F1 is **closed**, not deferred; F4 is **in scope**, §4.9; the DPR accountability roster's plain status filter is named as F5, deferred); the DPR-side fix for F5.


## Dated corrections, 19 Sep 2026 (rev10) — every change in this pass

| # | Earlier text (retracted / added) | rev10 result | Where |
|---|---|---|---|
| 1 | ~~the single add-engineer plan at `eb8a9c2`~~ | **Split out of the add-engineer plan at `eb8a9c2` after external review**, by content only. **The episodes design (D15) reverses D10 and has NOT been externally reviewed in this shape.** Header states contains / does not contain / dependency (slice 2 depends on slice 1's `users` columns and authorisation helper). | header |
| 2 | ~~rev9 D20: "not specified; gap semantics proven in the pure T23 instead"~~; ~~rev9 §4.9 tests intro "A back-dating seed helper … not specified; D20"~~ | **D20 changed (Aravind): gap semantics proven in a pure test do not prove the board query — a pure test cannot go red for a query bug.** **T43** specified: a database-level test that a gap date is excluded from the board, **shown failing first three ways** (natural red; an open-episodes-only read; a mis-keyed read). Seeding mechanism flagged: UNKNOWNS #50. | §4.9, Decisions |
| 3 | ~~rev9 D18 "flagged default"~~ | **ACCEPTED (Aravind)**, with the inconsistency named: under lazy materialisation, engineers WITH episodes get the join-side rule and engineers WITHOUT do not, so the board behaves two ways until every engineer has an episode (no eager backfill planned). | §4.9, Decisions |
| 4 | ~~rev9 D19 "flagged, review-package decisions"~~ | **ACCEPTED as review-package defaults (Aravind).** | Decisions |
| 5 | ~~T35 (the classifier offers reactivation) — dropped by the rev9 rewrite of §4.9~~ (found while inventorying `eb8a9c2` against `1765d21`) | **Restored verbatim from rev8 (`git show 1765d21`).** It names no removed column, so it needed no adaptation. | §4.9 |
| 6 | ~~§13 "REQUIRED BEFORE THE BUILD PACKAGE — split this plan"~~; ~~UNKNOWN #44~~; ~~§12 "The plan split (§13) creates files under `docs/plans/add-engineer/`"~~ | Done as two documents; §13 and #44 closed; §12 sentence struck. | §13, §12 |
| 7 | (added) header "Consequences of the split for slice 2" | (i) slice 2 **redefines** slice 1's `add_engineers_to_project`; (ii) engineers added by slice 1 have **no episode**; (iii) slice 2 is its own migration (ASSUMED `049`); (iv) the board behaves two ways (D18). Recorded as consequences, not decisions. UNKNOWNS #51–#53. | header |
| 8 | (added) UNKNOWNS #50–#54 | T43 seeding mechanism; add-function redefinition gate; slice-1-added engineers; episodes not externally reviewed in this shape; T35 restoration. | UNKNOWNS |
| 9 | ~~§12 "This revision's diff: exactly `docs/plans/add-engineer-plan.md`"~~ | The split's diff is both plan files. | §12 |

## Dated corrections, 19 Sep 2026 (rev9) — every change in this pass

| # | Earlier text (retracted) | rev9 result | Where |
|---|---|---|---|
| 1 | ~~rev8 D15 default: keep `deactivated_by/at` on `users`, add `reactivated_by/at` (five columns → seven), interval rule~~; ~~D10 "deactivated_by/at on `users`, one slot"~~ | **D15 settled by Aravind: an `engineer_episodes` table.** D10 is **reversed**: `deactivated_*` and `reactivated_*` are not created. **The attribution design changed after external review signed off on the five-column version**; the table re-enters the gate. Not both shapes. | §2.8, §6 |
| 2 | ~~rev7/rev8 board rule `isAfterDeactivation(status, deactivatedAt, reactivatedAt, logDate)`, the "`deactivated_at` NULL ⇒ legacy" and "`status === 'deactivated'` guard" bullets~~ | `isExpectedOn(episodes, logDate)`: no episodes ⇒ expected (legacy); else expected iff an episode's IST-day span contains the date (opening day in, closing day out). A date in a gap, before the first episode or after the last close is not expected. | §4.9 |
| 3 | ~~rev8 §2.10 "What happens to `deactivated_by`/`deactivated_at` … keep them and add `reactivated_*`"; "the limit this leaves: one episode only"~~ | A reactivation **opens a new episode and touches no earlier one**; the one-episode limit **no longer exists**. The reactivation-day morning chip (leave-side twin of Edge 1) stays named, not built. | §2.10 |
| 4 | ~~rev8 §2.11 "no `daily_logs` row with a submitted timestamp" as the "no inbound" predicate~~ | **No `whatsapp_sessions` row for the number.** Every per-inbound record examined (table in §2.11): `processed_messages` has no phone; delivered/read are not recorded; `messaging_blocked` is never set. Hindrance-only repliers are no longer false positives. **Residual classes recorded:** FP-1 (static-ack-only inbound), FN-1 (a replying stranger hides a typo), FN-2 (fragile if a cron ever starts a session — T37 (iv)). | §2.11 |
| 5 | ~~rev8 UNKNOWN #33 "which database a Preview reads … a Preview built before 048 would error"~~ | **Not determinable from the repo** (evidence printed); consequence stated: production order unchanged, but a Preview visit is no evidence of 048 and step 5 must be verified on the production deployment; a possible prod service-role key in Preview is a security question. Dashboard checklist given. Kept in UNKNOWNS. | §4.9 |
| 6 | ~~rev8 F5 header "REAL BUG, LATENT, DEFERRED"~~ | Recorded as **a real bug on an unreachable path — latent, not live**, with the evidence (`generate-one-dpr.ts` never reaches `dispatch.ts:418`; `accountability.ts` has no live caller). Aravind's "arguably correct" stays struck. | §2.9 F5 |
| 7 | ~~rev8 §13-less: plan-size WARN noted only in a commit message~~ | **Split recorded as a required step before the build package** (§13): themes and files named; **not done here**; the WARN stays. | §13 |
| 8 | ~~rev8 N1 "widens the exposed surface by seven `users` columns"~~ | Three `users` columns, plus a **new table with its own explicit grants** (§2.8). | §4.10 |
| 9 | ~~rev8 tests T33/T34 (fixtures via `deactivated_at`/`reactivated_at`); rev7 T23/T24c (IST edge on `deactivated_at`)~~ | Rewritten on episodes. Integration tests create episodes by calling the real functions (`service_role` holds `SELECT` only); multi-day gaps and IST edges are proven in the pure T23. New: T24f, T40–T42. A back-dating seed helper is **not** specified (D20). | §4.9 |
| 10 | ~~rev8 `users` "not set" list including `deactivated_by, deactivated_at`~~ | Removed; §2.6 gains the episode row. | §2.6 |

## Dated corrections, 19 Sep 2026 (rev8) — kept (`git show 1765d21:docs/plans/add-engineer-plan.md`)

| # | Earlier text (retracted) | rev8 result | Where |
|---|---|---|---|
| 1 | ~~rev7 §4.9, §6 and the banner: "048 must be applied … **before the app change deploys**"~~ | **Merge is the deploy** (printed: `035-lockstep-runbook.md:162-164`, `2026-q3-week-4-migrations.md:519-523`; no deploy step in `ci.yml`/`vercel.json`). Order: 048 → test-db → CI green (pinned run URL, `headSha` = PR HEAD) → 048 → prod → **THEN merge**. DOWN: **revert commit (merged) first, then the DOWN.** | §4.9, §6 |
| 3 | ~~rev6/rev7: "reactivating from the dashboard" deferred; "no reactivation path is designed" (UNKNOWNS #24); D10 "one slot; no history"~~ | **Reactivate ships in this slice** (Aravind): third SECURITY DEFINER function. Because neither "clear" nor "keep the first record" preserves "past dates unchanged", the migration gains **`reactivated_by`/`reactivated_at` (five columns become seven)** and the board rule becomes an interval. Departure from settled D10 flagged as **D15**. | §2.10, §4.9, §6 |
| 4 | ~~rev7 R5: any same-project engineer classifies `already_on_this_project`~~ | A **deactivated** same-project engineer gets its own outcome `deactivated_on_this_project` (`{idx, status, user_id}`, same tenant and project only) and the flow offers reactivation. Cross-tenant stays generic. | §2.1, §2.10, §4 |
| 5 | ~~rev7 §4.9: `deriveHalfStatus` "unchanged"; the Today loop's separate `if (e.afterDeactivation) continue`~~ | The D14 (i) kept card would show an amber evening chip from 19:15 IST. **One branch in `deriveHalfStatus`** (optional 7th parameter `notExpected`, new state `not_expected`, no chip, no label). Call sites incl. **`log-detail-view.tsx:431,439`**, which rev7 missed. The Today loop's separate `continue` is **struck** — single source of truth. | §4.9 |
| 6 | ~~rev7 F5 "Reading, not run … whether a past-date regeneration path exists is UNKNOWN (#23)"~~ | **Closed with evidence, and a premise of the instruction corrected:** `generate-one-dpr.ts:6,38` takes a past date but **does not reach** `dispatch.ts:418` (its imports `:29-35`). The bug is real, latent (no repo enqueuer supplies a past date), deferred; `accountability.ts:155-160` has no live caller. | §2.9 F5 |
| 9 | ~~rev7 §9 / UNKNOWNS #11 framing of `DEACTIVATE_CONFIRM`~~ | The plan never stated irreversibility (grep: none). Because reactivate ships, `DEACTIVATE_CONFIRM` needs **no irreversibility fact** — recorded for whoever writes the wording. | §9 |
| 10 | ~~rev7 T16 "all three functions"; §8 "The three functions bypass RLS"; §6 "(1)–(6)"~~ | Four functions (helper + `add` + `deactivate` + `reactivate`). | §6, §7, §8 |
| 11 | ~~rev7 §4.9 T23 signature `isAfterDeactivation(status, deactivatedAt, logDate)`~~ | Adds `reactivatedAt`: `isAfterDeactivation(status, deactivatedAt, reactivatedAt, logDate)`. | §4.9 |

## Dated corrections, 19 Sep 2026 (rev7) — kept (`git show f407ba1:docs/plans/add-engineer-plan.md`)

| # | Earlier text (retracted) | rev7 result | Where |
|---|---|---|---|
| 1 | ~~rev6 banner "Open decision — D13"; §4.8 D13 "OPEN; not chosen" and its option table 1–4; Decisions row "OPEN — product decision; not blocking the build"~~ | **D13 settled by Aravind: in this slice, dated, not a status filter.** The option table is superseded (`git show eff12a0:docs/plans/add-engineer-plan.md`, §4.8). The rule, the exact IST comparison, the changed function and predicate, the three surfaces' day-after behaviour and the tests are in §4.9. | §4.9, Decisions |
| 2 | ~~rev6 §4.8 option 2 "hide by `users.status`: `users!inner(…)` + `.eq('users.status','active')`" presented as the small change~~ | Stated as **wrong** for this requirement, not merely costly: `users.status` is current state and the board iterates the roster **per date**, so it erases every past card. Kept only as the mutation that T24b must turn red. | §4.9 |
| 3 | ~~rev6 §2.9 F1 "RECORDED, NOT FIXED, ACCEPTED LIMIT", its "Exposure" sentence and its "Why not fixed" paragraph; Scope "F1 deferred"~~ | **F1 is CLOSED, not accepted.** Step 5 exists only when the morning answer was NO; a NO writes no `daily_logs` row (`033:61-65`), so the `ON CONFLICT DO UPDATE` has no recorded attendance to overwrite; steps 2–4 never touch attendance and a sweep test asserts it is preserved. Only the **unproven** remainder is in UNKNOWNS #5. | §2.9, UNKNOWNS |
| 4 | ~~rev6 §12 / Scope "`lib/daily-logs/` is a directory this slice was told not to touch; D13 pending"~~ | **Scope change (Aravind):** `lib/daily-logs/query.ts` is in scope for D13 only. No other file under `lib/daily-logs/` is touched. | §12 |
| 5 | ~~rev6 §9 "D13's Option 4 would add one blank constant (a "Deactivated" state label) if chosen"~~ | **Adds none.** D13 uses `ENGINEER_STATUS_DEACTIVATED`, already named in §9. | §9 |
| 6 | ~~rev6 UNKNOWNS #21 "a component I did not open (e.g. shared UI under `components/`) could too"~~; ~~#8 "`test/dash-03-board*.test.ts` … not read"~~ | Narrowed by evidence: no file under `components/` contains `project_members`; no non-literal `.from(<table>)` exists (only storage-bucket calls); no view mentions it (probe pg_1); two functions do, neither lists engineers to a screen (probe pg_2). Two of the four board test files were read. What is **not** ruled out is in UNKNOWNS #21. | §4.9, UNKNOWNS |
| 7 | ~~rev6 §4.8 "legacy deactivated rows have NULL — their meaning is undefined"~~ | **Defined:** `deactivated_at` NULL ⇒ the rule does not apply (unchanged behaviour). `users.status` has **three** values, `pending`/`active`/`deactivated` (probe pg_4), so the guard is `= 'deactivated'`, never `<> 'active'`. | §4.9 |
| 9 | ~~rev6 F4 table read as a complete list of surfaces~~ | Still the pre-rev7 behaviour; §4.9 gives the **post-rev7** behaviour for the same three surfaces and adds the heading-count composition (`dashboard/page.tsx:184`: hindrances + missing check-ins + needed-tomorrow). | §2.9, §4.9 |

## Dated corrections, 19 Sep 2026 (rev6) — kept

| # | Earlier text (retracted) | rev6 result | Where |
|---|---|---|---|
| 6 | ~~rev5 §2.9 "the membership keeps the partial-index slot"~~ | Struck: there is no index. The membership row simply stays. | §2.9 |
| 9 | ~~rev5 §2.9 F1 (step 5 described as "an INSERT … or overwrite")~~ | Extended (Part C): what the overwrite means for an already-recorded attendance, from the printed writers. | §2.9 |
| 10 | ~~rev5 F4 "the fix is in `lib/daily-logs/`… Deferred"~~ | Traced surface by surface; three surfaces affected; **D13** with costs. | §2.9, §4.8 |

## Dated corrections, 19 Sep 2026 (rev5) — kept

| # | Earlier text (retracted) | rev5 result |
|---|---|---|
| 4 | ~~rev4 §2.9 F1 "yields a `daily_logs` write attributed to a deactivated engineer"~~ | Precise blast radius; accepted limit; `ON CONFLICT DO UPDATE` nuance. |
| 5 | ~~rev4 UNKNOWN #5 "Daily Logs view"~~ | Answered: the board shows it; F4. |

## Dated corrections, 19 Sep 2026 (rev4) — kept

| # | Earlier text (retracted) | rev4 result |
|---|---|---|
| 1 | ~~rev3 heading "Dated corrections, 18 Sep 2026 (rev3)"~~ | Wrong date: rev3's log starts `Sat Sep 19 00:01:34 IST 2026`, commit `2026-09-19 00:15:46 +0530`; 18 Sep only in UTC. |
| 3–5 | ~~rev3 D8/D9/D10 "open"; "the action refuses if attestation absent"; three-parameter signature~~ | CHECK settled (RLS insert policy places no restriction on the inserted row's `role`); D9 record-not-enforce (`consent_attested`, fourth parameter); D10 `deactivated_by/at`. |
| 6 | ~~rev3 T12 "the repo already uses `it.fails`"~~ | **Wrong**: no executable `it.fails` in `test/`; behaviour **verified by running it** on vitest 3.2.7 (re-run in the rev6 log). |
| 7–14 | session cleanup untraced; `pg_temp`; §7.3 "assumed no cron"; R7 split; matrix rows fixture-only; CHECK optional; digit strings; new items | Traced (F1–F3); `search_path=public` only; shown/not-shown breakdown; `registered_no_project`; rows 3–8 fixture-only; CHECK definite; residual-risk statement (§4.6). |

## Dated corrections, ~~18 Sep 2026~~ **19 Sep 2026** (rev3) and 18 Sep 2026 (rev2) — kept (dates verified)

| # | Earlier text (retracted) | Result |
|---|---|---|
| rev3 1 | ~~rev2 §9 "`// Tamil owed, NOT approved`" citing `lib/photos/copy.ts:13-20`~~ | Mis-citation: that comment marks *approved English awaiting Tamil* (`copy.ts:3-6`); unapproved wording uses `// Wording owed, NOT approved`. |

## 0. Findings that shape the plan — read first

3. **`users.status` is `NOT NULL DEFAULT 'active'`** (probe d; `012_…sql:45-46`); the gate is `route.ts:159` → `reactivation.ts:29-33`.
4. **`authenticated` cannot write `users`**: no INSERT (probe k, `015:114`), UPDATE only on `full_name, avatar_url` (`015:105`), only UPDATE policy is own-row (probe j). Add and deactivate both need definer functions; no service client (g4).
6. **Migration number 048** is free (`origin/main` migrations end at 047; reservations end at 048, "RELEASED, NEVER USED … 048 is free").
7. **Deactivation propagates through `users.status`** (§2.9); findings F1–F4.
10. ~~**The Daily Logs board and the Today page keep showing a deactivated engineer as failing, every day** (F4, D13).~~ **Settled (rev7):** the board and Today stop expecting a deactivated engineer from the deactivation day on, and keep all history (§4.9). The change touches `lib/daily-logs/query.ts` and **depends on migration 048 being applied first**.

## 1. Authorisation — lives in slice 1

The D6 role/authority gate (§1 of slice 1) and the internal helper (§2.7 of slice 1) are reused **unchanged** by `deactivate_engineer` and `reactivate_engineer`; they are not restated here. See slice 1 §1 and §2.7.

## 2. The lifecycle functions and the episodes table

### 2.1 (payload row for the classifier outcome — the rest of §2.1 is in slice 1)

| status | fields | note |
|---|---|---|
| `deactivated_on_this_project` | `idx, status, user_id` | **same tenant AND same project only** (§2.10); the `user_id` is what the reactivate control calls |

### 2.6 (the `engineer_episodes` row written by the add path, from slice 2 onward — the `users` and `project_members` rows are in slice 1)

**`engineer_episodes` row (rev9, §2.8):** `id`/`created_at` defaults; `tenant_id` = the derived tenant; `user_id` = the new id; `project_id` = `p_project_id`; `opened_at` = the function's `now()` — **the same instant as `users.registered_at`**; `opened_by` = the resolved caller's `users.id`; `opened_via` = `'added'`; `closed_at`/`closed_by`/`closed_via` NULL.

### 2.8 Attribution and episodes — D10 REVERSED, D15 SETTLED (rev9)
**Confirmed absent** (grep c1/c3; probe s; probe r): none of `registered_by`, `registered_at`, `consent_attest*` exists, and the name `engineer_episodes` is unused in migrations, generated types, `docs/schema.md` and the reservations file (grep, rev9 log). **Cannot be reconstructed later:** after apply nothing else records who created, deactivated or reactivated an engineer; the only other trace is request logging outside the database, which this repo does not read.
**D9:** a direct caller can pass any value, so a refusal buys nothing, while the recorded claim plus `registered_by`/`registered_at` answers "who confirmed this, and when" later. It records **the caller's claim**, not proof of consent; the screen's apply control is disabled until ticked (UI only).
**What changed after sign-off, stated plainly.** ~~D10 (settled): `deactivated_by`/`deactivated_at` on `users`; rev8: + `reactivated_by`/`reactivated_at`.~~ **Reversed by Aravind (rev9): those four columns are NOT created, and no shape of them survives in this plan.** External review signed off on the **five-column** version (`registered_by`, `registered_at`, `consent_attested`, `deactivated_by`, `deactivated_at`). **The attribution design changed after that sign-off:** `users` keeps three columns and a new table carries the rest. The reviewer's Design GO therefore does **not** cover the table, its grants, its lifecycle or the board's new read of it — those re-enter the gate (§6).
**Why (Aravind's reasoning, recorded).** The board's real question is "was this engineer expected on this date" — an **interval** question. Columns answer it for the most recent interval and are wrong about every earlier one — the limit rev8 found in its own design before anyone hit it. Episodes answer it exactly, and the same record serves the deferred reassign and site-transfer work: closing an episode on one project and opening one on another is the same two operations.
**Where each fact lives.** **On `users`:** `registered_by`, `registered_at`, `consent_attested` (pairing CHECK `(registered_by IS NULL) = (registered_at IS NULL) AND (registered_by IS NULL) = (consent_attested IS NULL)`, precedent `036…:232-244`; composite same-tenant FK `(registered_by, tenant_id) → users (id, tenant_id)`, `017:94-100`). They are facts about **the number and its holder**: a consent attestation is a claim about a handset, made once; a deactivated-then-reactivated engineer is the same person on the same number, so reactivation neither overwrites the attestation nor is assumed to need a fresh one (UNKNOWNS #24); and the unreachability report keys on when the **number** was registered (§2.11). **On `engineer_episodes`:** *when the engineer was expected on a project* and *who opened or closed that*. **One intentional duplication:** the add path writes `users.registered_at` and the first episode's `opened_at` from the same `now()` of one transaction — one event recorded twice by design, asserted equal in T10, not two facts that can drift. Putting everything on the episode was rejected: it would make consent read as per-project and lose the person-level record if episodes were ever archived.
**Table `public.engineer_episodes`** (new):
| column | definition | note |
|---|---|---|
| `id` | `uuid PRIMARY KEY DEFAULT gen_random_uuid()` | |
| `created_at` | `timestamptz NOT NULL DEFAULT now()` | |
| `tenant_id` | `uuid NOT NULL` | FK to `tenants (id) ON DELETE CASCADE` (as `whatsapp_sessions`, `001:89`) |
| `user_id` | `uuid NOT NULL` | the engineer |
| `project_id` | `uuid NOT NULL` | the project they are expected on |
| `opened_at` | `timestamptz NOT NULL` | set explicitly to the function's `now()`, never defaulted |
| `opened_by` | `uuid` | the resolved caller's `users.id` (never `auth.uid()`); NULL = backfill or a since-deleted caller |
| `opened_via` | `text NOT NULL CHECK (opened_via IN ('added','reactivated','backfill'))` | TEXT + CHECK, never ENUM (`CLAUDE.md` §6); adding `'reassigned'`/`'transferred'` later is a CHECK edit |
| `closed_at` | `timestamptz` | **NULL = the episode is open** |
| `closed_by` | `uuid` | |
| `closed_via` | `text CHECK (closed_via IN ('deactivated','backfill'))` | |
CHECKs: `(closed_at IS NULL) = (closed_via IS NULL)`; `closed_at IS NULL OR closed_at >= opened_at`.
**Tenant scoping — composite same-tenant FKs, in the style of `project_members` (`017:94-106`, explicit `ON UPDATE NO ACTION`):** `(user_id, tenant_id) → users (id, tenant_id) ON DELETE CASCADE`; `(project_id, tenant_id) → projects (id, tenant_id) ON DELETE CASCADE`; `(opened_by, tenant_id)` and `(closed_by, tenant_id) → users (id, tenant_id) ON DELETE SET NULL (opened_by)` / `(closed_by)` — the column-list form (PG15+; prod and test-db run 17.6, `CLAUDE.md` §7), because a plain `SET NULL` would also try to null `tenant_id`. `MATCH SIMPLE`: a NULL actor skips the check (`017:83-87`), and that is what allows a backfill row. **Referential actions — the production reason, not a fixture reason (`§3.6`):** `project_members.user_id` already cascades (`017:100`), so deleting a user removes their membership; an episode that blocked or outlived that delete would contradict it. (Users are never hard-deleted in prod, `031:492-494`; the choice is parity, not a licence.) **Review-package decision, flagged (D19):** RESTRICT was the alternative (`031`'s choice for a billed ledger); it is rejected here because episodes carry no billing.
**Indexes.** `uq_engineer_episodes_one_open ON (user_id) WHERE closed_at IS NULL` — the enforcement below; `idx_engineer_episodes_project ON (project_id, opened_at)` — **the board's access pattern** (§4.9: `project_id = ANY(…)`); `idx_engineer_episodes_user ON (user_id, opened_at)` — the FK column and the detail-page lookup.
**RLS and grants (new table — gate (b)).** RLS enabled; one policy, `engineer_episodes_select FOR SELECT TO authenticated USING (tenant_id = get_user_tenant_id())` (the shape of `project_members_select`, `002:114-116`); **no INSERT/UPDATE/DELETE policy.** `REVOKE ALL ON engineer_episodes FROM PUBLIC, anon, authenticated, service_role; GRANT SELECT ON engineer_episodes TO authenticated; GRANT SELECT ON engineer_episodes TO service_role` — `service_role` is **named** (`CLAUDE.md` §0 rule) and gets `SELECT` only (the report and crons read; **nothing writes except the three definer functions**, whose owner bypasses RLS and grants). Contrast `031:557-558`, which grants `INSERT, UPDATE` because the send primitive writes that ledger. T41 proves the negative space on the **real** database.
**Lifecycle.**
- **Opens on add:** `add_engineers_to_project` inserts, per row, the `users` row, the `project_members` row **and one episode** (`opened_by` = caller, `opened_via = 'added'`, `opened_at = now()`).
- **Opens on reactivate:** `reactivate_engineer` sets `users.status = 'active'` **and** inserts an episode (`'reactivated'`).
- **Closes on deactivate:** `deactivate_engineer` sets `users.status = 'deactivated'` **and** closes the engineer's open episode on this project (`closed_at = now()`, `closed_by` = caller, `closed_via = 'deactivated'`).
- **Legacy engineers — every engineer that exists today has NO episode — are materialised lazily, on first touch, never by a migration backfill.** Deactivating a legacy *active* engineer first inserts an episode `(opened_at = project_members.created_at, opened_by NULL, opened_via 'backfill')`, then closes it as above. Reactivating a legacy *deactivated* engineer (deactivated before 048, so no recorded start) inserts a **closed** `'backfill'` episode `[project_members.created_at, R)` — the board keeps showing the pre-048 gap as it does today ("past dates unchanged") — and then the new open episode at R. **Why lazy, a finding:** an eager `INSERT … SELECT` backfill would create one open episode **per engineer membership**, and the real, defended state "one engineer, two `engineer` memberships" (§4.10) would violate `uq_engineer_episodes_one_open` and **abort the migration**; prod's count of such engineers is unread (§11 `n3`). Lazy materialisation writes no data in the migration, so the additive-backfill rule machinery (`CLAUDE.md` §6) is not tripped. Alternative, not chosen: eager backfill guarded by a pre-check that no engineer has two memberships.
**What enforces "at most one open episode per engineer".** **The database:** the partial unique index above — race-proof; the second of two concurrent opens gets `23505`. **The functions:** after the §2.7 helper resolves the caller, each locks the target `users` row `SELECT … FOR UPDATE` before reading or writing episodes, so concurrent deactivate/reactivate serialise; both then write `users.status` and the episode in **one transaction**. **Not DB-enforced, named:** non-overlap of *closed* episodes (an `EXCLUDE USING gist` needs `btree_gist`; availability unread, UNKNOWNS #39) — the functions always open at `now()` at or after the last close, and the CHECK forbids `closed_at < opened_at`. **Two records must agree** (the price of keeping `users.status`, which every roster reads): invariant **I1** — `status = 'active'` with any episode ⇒ exactly one open; `status = 'deactivated'` ⇒ none open. The functions maintain it; T34 asserts it after every step; §11 `e1` is the drift probe for prod. **This index is not the D12 index:** it is on a table only the definer functions write, so the 7 test files of §4.7 (which insert `project_members` rows directly and never episodes) are untouched; the D12 rider stays as pinned (§4.10).

### 2.9 Deactivate — the SECOND SECURITY DEFINER function (`deactivate_engineer`)
**Contract.** `(p_project_id uuid, p_user_id uuid) → jsonb` `{ status: 'deactivated' | 'already_deactivated' }`. Same helper, gate, tenant binding and grants. Target: `users u JOIN project_members pm` with `u.id = p_user_id`, `u.role='engineer'`, `u.tenant_id` = derived tenant, `pm.project_id = p_project_id`, `pm.role='engineer'`; **not found → `no_data_found`**. In **one transaction** (the target `users` row locked `FOR UPDATE`): `UPDATE users SET status = 'deactivated'` **and close the engineer's open episode on this project** (`closed_at = now()`, `closed_by` = the caller, `closed_via = 'deactivated'`; a legacy engineer with no episode first gets one materialised from `project_members.created_at`, §2.8) — ~~rev8: one UPDATE writing `deactivated_by`/`deactivated_at`~~;
already deactivated → no write, no overwrite. `'deactivated'` is permitted by `users_status_check` (probe h). **Does NOT:** delete; touch `whatsapp_number`, `messaging_blocked`, `project_members`, or `whatsapp_sessions`; send; reactivate (§2.10 is its own function); use dynamic SQL.
**KNOWN LIMIT — the number stays held** (`UNIQUE (whatsapp_number)`, `001:44`, probe h). ~~and the membership keeps the partial-index slot~~ (rev6 #6: no index). **A mistyped number cannot be corrected from the dashboard; the only repair is manual SQL.**
**Deactivation takes effect with no further change — printed source.** Webhook: `decideInboundGate` returns `gated_noop` for `status !== 'active'` (`reactivation.ts:29-33`), header forbids silent reactivation (`:11-14`), clear-half re-asserts `status='active'` (`:64`); `route.ts:159`, `twimlEmpty()` at `:161-163`. Outbound: `fetchActiveEngineers` filters `.eq('users.status','active')` (`roster.ts:168`), used by both exported rosters (`:203`, `:248`). Also filtering: `checkin-escalations/roster.ts:65`, `dpr-generate/route.ts:80`, `accountability.ts:159`, `dpr/dispatch.ts:417-418`. Why `status`, not `messaging_blocked`: the flag would make the gate return `reactivate` (`:36-38`).

### Session trace and PM-visible findings — named, not fixed (except D13 pending)
- **Inbound from a deactivated engineer:** `route.ts:159-163` returns `twimlEmpty()` before idempotency, project resolution and any session call.
- **F1 — the morning sweep never reads `users` — CLOSED (rev7), ~~RECORDED, NOT FIXED, ACCEPTED LIMIT~~.**
  **Why it is closed, in four sentences.** Step 5 exists **only when the morning answer was NO**; the file header says a NO "advances straight to step 5 with no write" (`033:61-65`), so **no `daily_logs` attendance row exists** for the `ON CONFLICT … DO UPDATE` to overwrite; steps 2–4 **never touch attendance** (`033:235-245`) and `test/unit/morning-cutoff-sweep.test.ts:209-224` asserts a step-2 attendance "already present is preserved untouched"; and the only two writers of attendance are `apply_morning_flow_turn` and this sweep (grep `a1`). So the sweep cannot destroy a recorded attendance by any path I printed. **What is NOT closed is one unproven premise, held in UNKNOWNS #5 only:** whether any path can put a *recorded* attendance beside a step-5 session — and that, if one existed, the loss would be silent (the sweep writes no audit row). A closed risk is not filed as an open one; an unproven premise is not filed as closed. The sweep's ordinary output — one `'absent'`, defaulted row for a session already parked at step 5 — is unchanged by deactivation and, on the deactivation day, is **visible** under §4.9 (a log exists, so the card is kept), not hidden.
  *The detail below is the trace that closes it; its earlier labels are kept struck.* Verified from `033`: the sweep loops `SELECT * FROM whatsapp_sessions WHERE current_flow = 'morning'` (`033:198-200`); the printed body reads only `whatsapp_sessions`, `project_members`, `daily_logs` — grep `u1`: zero non-comment lines mention `users`; `u2`: the only targets.
  **Blast radius, precisely.** (1) It acts **only on an existing session**; a deactivated engineer gets **no new morning trigger** (`roster.ts:168`). (2) Only an engineer with **exactly one** membership is processed (`033:220-233`). (3) Steps 2–4 only **UPDATE `morning_submitted_at` on a row that already exists** (`033:235-245`). (4) **Only step 5 writes attendance** (`033:265-283`). (5) The session then closes (`033:306-316`), so it cannot repeat.
  **The nuance rev4 missed — step 5 can OVERWRITE.** It is `INSERT … ON CONFLICT (project_id, engineer_id, log_date) DO UPDATE SET attendance = EXCLUDED.attendance, attendance_defaulted = …, attendance_raw = …, is_holiday = …, morning_submitted_at = …` (`033:273-282`). So if a row already exists for that `(project, engineer, day)`, its attendance fields are **replaced** by `'absent'`, `attendance_defaulted = true`, `attendance_raw = NULL`, `is_holiday = false`, and the submission time.
  **What that means for a real engineer's already-recorded attendance on the day of deactivation** (from the printed writers): attendance is written only by `apply_morning_flow_turn` (`030:550-557,565-570`, redefined `035:226-495`, upserts at `035:383-395`) and by this sweep — grep `a1`: no `app`/`lib` code writes it and the evening RPC has no attendance write. (a) An engineer who **answered YES** has `'present'` recorded and a session at steps 2–4 or complete: steps 2–4 **never touch attendance** (`033:235-245`; the sweep test `:209-224` asserts "already present is preserved untouched"), so **their recorded attendance is preserved.** (b) Step 5 means the morning answer was **NO**, and the header says a NO "advances straight to step 5 with no write" (`033:61-65`), so **no attendance row exists to lose** — the sweep creates one. (c) The only route to overwriting a *real recorded value* is a row **and** a step-5 session on the same log day; the header names one route (evening wrote the row first, `033:65-67`), but the evening RPC writes no attendance, so that row would hold NULL attendance. I found **no path** that puts a recorded `'present'`/`'site_holiday'` beside a step-5 session and did **not prove none exists** (UNKNOWNS #5).
  **If it did happen, the loss would be silent:** the sweep writes no audit row (targets in `u2` exclude `daily_log_edits`, which only the PM-correction RPC writes, `019`). ~~**Exposure: one phantom log — or, in the unproven case, one overwritten attendance — on one day, for an engineer deactivated while parked at step 5.**~~ ~~**Why not fixed:** … a full package and rehearsal — out of proportion to that exposure. **Accepted limit.**~~ — struck (rev7 #3): nothing is being accepted, because on the printed evidence nothing is at risk; no fix to `033` is proposed or needed.
- **F2 — evening and hindrance sessions are parked indefinitely.** The sweep covers `'morning'` only (`033:199`); `expires_at` is read by nothing (grep s3; `033:32-34` calls it dead); nothing deletes session rows (grep s4); the only reset is BOT-07's next-day wipe inside `acquire_and_transition_session` (`012:115-121`), reachable only by an inbound past the gate or a cron send — neither can happen for a deactivated engineer.
- **F3 — the engineer with no membership.** Cannot be deactivated from the dashboard (the function needs an engineer-role membership; the list is membership-based, `projects/[id]/page.tsx:56-58`), so **the admin sees nothing to click**. If `active`, that engineer texts in and gets `ZERO_MEMBERSHIPS_REPLY` (`route.ts:244-247`, `project-resolution.ts:71-72`: ask your Project Manager to add you) — but the PM cannot add them (R7a) or deactivate them: a dead end until the reassign slice or manual SQL. Test-db holds **1,870** such engineers (probe z; fixtures); prod unread (§11 `w`).
- **F4 — DEACTIVATE IS INCOMPLETE ON THE PM'S OWN SCREENS — SETTLED in rev7 (D13, §4.9); the table below is the PRE-rev7 behaviour, kept as the "before".** *Product consequence, in plain words:* **deactivate stops the messaging but leaves the engineer visible on the PM's own screens — the one place a PM would look to confirm it worked.** Traced surface by surface (all printed in the log):
  | Surface | Roster source | Reads `users.status`? | What the PM sees the day after |
  |---|---|---|---|
  | **Daily Logs** (`daily-logs/page.tsx:50` → `getDailyLogsBoard`, `lib/daily-logs/query.ts:159-163`) | `project_members` where **membership `role='engineer'`**; selects `users(id, full_name, messaging_blocked, whatsapp_number)` | **No** | The engineer's **card stays** (`engineer-card.tsx:120`). Morning chip "Awaiting morning" (muted) until **15:00 IST**, then **"Not checked in" in amber**; evening chip "Awaiting evening" until **19:15 IST**, then amber (`status.ts:125-153`, cutoffs `cutoffs.ts:64,89-90`). **No deactivation marker exists** — the card only knows `messaging_blocked` (`status.ts:116-123`), which deactivate does not set. Every past date without a log shows amber "Not checked in" (`status.ts:130-137`). Same every day, indefinitely. |
  | **Today** (`dashboard/page.tsx:116` → same board; loop `:140-174`) | the same board | **No** | From **10:30 IST** (morning escalation, `cutoffs.ts:53`) and **19:15 IST**, a **"Needs your attention"** card: "‹name› hasn't sent a morning/evening check-in" with an amber chip (`dashboard/page.tsx:151-172,299-301`), which also feeds the page heading's count of "things that need you" (`:176-201`). Every day, indefinitely. |
  | **Project detail → Team Members** (`projects/[id]/page.tsx:56-58,129-133`) | `project_members` (all roles) with `users(id, full_name, role)` | **No** | The engineer's row is **identical** to an active member's — name, user role, project role; no marker. |
  | DPR archive (`dprs/page.tsx:61-81`) | the `dprs` table (project scope from the PM's memberships, `:32`) | n/a | Past DPRs stay; **no new ones** (roster filters status, `dpr-generate/route.ts:80`). Not roster-driven. |
  | Hindrances (`hindrances/page.tsx:45` → `hindrance/queue.ts:169-172,288-291`) | the `hindrances` table; PM scope `role='pm'` | n/a | Past reports stay; none new. Not roster-driven. |
  | Needed tomorrow (`query.ts:549+`) | `daily_logs` in a two-day window | n/a | The engineer's last dependency text shows for its window, then disappears. Not roster-driven. |
  | Check-in escalations | roster with `.eq('users.status','active')` (`checkin-escalations/roster.ts:65`) | **Yes** | Excluded — no escalation for them. |
  **The inconsistency (pre-rev7):** the system's own escalation, DPR and messaging logic treat the engineer as out, while the two screens a PM actually reads (Daily Logs, Today) keep counting them as failing. This is not new to deactivation — `messaging_blocked` engineers get a dedicated chip because the same problem was met there (`status.ts:103-123`) — but `deactivated` has no equivalent.
- **F5 — A REAL BUG ON AN UNREACHABLE PATH — LATENT, NOT LIVE, DEFERRED (rev8; labelled so in rev9).** ~~rev7: "Reading, not run … whether a past-date regeneration path exists is UNKNOWN (#23)".~~ Two DPR-side reads use `users.status` as a plain current-state filter — the wrong-rule class §4.9 rejects — and the evidence now says where each is reachable:
  **(a) `lib/dpr/dispatch.ts:418`** (`stillActiveMember = membership !== null && engineer?.status === 'active'`, **no date comparison**) and **`:436-438`** (the `left_early` label, "left this project during the day"), inside `resolveCheckInStatus` (`:385`), which is called only from `handleDprGenerateJob` (`:140-141`; defined `:63`). **Reachable by any `dpr_generate` job whose `payload.log_date` is a past date:** the handler takes the date from the payload, so for a since-deactivated engineer it labels a half `left_early` for a day **before** the deactivation, on which they were an active member throughout. The repo's **only enqueuer** is the cron (`route.ts:143`) with `logDate = istDateString(new Date())` (`route.ts:158`) — **today only, where the label is correct.** The **only past-date path in the repo** is `scripts/generate-one-dpr.ts` (`:6` usage line `<log_date>`, `:38` `process.argv`), documented as the by-hand re-run for a missed generation (`route.ts:40-41`) — **and it does not reach `resolveCheckInStatus`**: its imports (`:29-35`) contain neither it nor `dispatch.ts`, and its own comment says so (`:67`; the same in `scripts/preview-dpr.ts:145`). **Correction to the instruction's premise, recorded not smoothed:** Aravind's rev8 instruction cited `generate-one-dpr.ts:6,38` as the regeneration path on which `:418` mislabels; the printed imports show that script bypasses `:418`. The defect is **real code with a real trigger (any past-date job) that no repo path produces today**; the one regeneration path that exists does not run it.
  **(b) `lib/dpr/accountability.ts:155-160`** — `.eq('users.status','active')` in a function that takes a `log_date` (`:153`) it never uses to date-scope the roster. **No production caller:** `assembleAccountability` appears only at its definition and in comments (grep, log); `test/unit/accountability.test.ts:6` says it is "not exercised"; `docs/reviews/design-decisions-audit-batch2-2026-09-05.md:9,41` names it deferred (the accountability engine is Fast-Follow, `CLAUDE.md` §2). Same defect shape, **dead path today**; live only when that engine is built.
  **Precedents for the fix, both sides.** Join side, already date-aware: `dispatch.ts:427-428` compares `created.date === payload.log_date` where `created = istParts(new Date(membership.created_at))` (`joined_late`, a send-time threshold); the leave side is its mirror on episode `closed_at`/`opened_at` (§2.8, §4.9). "Keep who has data": `route.ts:91-101` unions the active roster with engineers who have `daily_logs` that day **regardless of current roster membership (S3)** — the rule D14 (i) applies on the board. **Same-day generation is correct as-is.** **rev9 record:** latent, not live — the trigger (a past-date `dpr_generate` job) is produced by **no path in the repo**, and the one past-date path (`generate-one-dpr.ts`) never reaches `:418`; the bug goes live only if a past-date enqueuer is ever added (the DPR-regeneration work is the likeliest), so this entry is the pointer that work must read. The fix is deferred (its own change under `lib/dpr/`, outside this slice) and recorded as a **bug**, not a judgement call. **UNKNOWN #23 is closed** by this evidence. (The words "arguably correct" do not appear in this plan — grep, log; the only "arguably" in the repo is an unrelated comment at `route.ts:44`. What is struck is rev7's hedge above.)

### 2.10 Reactivate — the THIRD SECURITY DEFINER function (`reactivate_engineer`) — ships in this slice (Aravind, rev8; rewritten on episodes, rev9)
**Contract.** `(p_project_id uuid, p_user_id uuid) → jsonb` `{ status: 'reactivated' | 'already_active' | 'not_deactivated' }`. **Same family as `deactivate_engineer`:** the §2.7 helper (caller resolved by `users.auth_id = auth.uid()`, tenant bound NULL-safely with `IS DISTINCT FROM`), the D6 gate (`admin`, or `pm` with a `pm` membership on this project), `SET search_path = public`, grants `REVOKE EXECUTE … FROM PUBLIC, anon, service_role; GRANT EXECUTE … TO authenticated`. Target: `users u JOIN project_members pm` with `u.id = p_user_id`, `u.role = 'engineer'`, `u.tenant_id` = the tenant derived from the project, `pm.project_id = p_project_id`, `pm.role = 'engineer'`; **not found → `no_data_found`**, indistinguishable from a foreign id. The target `users` row is locked `FOR UPDATE`. `status = 'active'` → `already_active`, no write. `status = 'pending'` → `not_deactivated`, no write (activating a pending user is ENG-01's job). `status = 'deactivated'` → **one transaction, two writes: `UPDATE users SET status = 'active'` and `INSERT` an episode** (`opened_by` = the resolved caller's `users.id`, `opened_via = 'reactivated'`, `opened_at = now()`; §2.8). A legacy-deactivated engineer with no episode first gets the closed `'backfill'` episode of §2.8.
**Does NOT:** delete; touch `whatsapp_number`, `messaging_blocked`, `project_members`, `whatsapp_sessions`, `consent_attested`, any earlier episode; send a message; use dynamic SQL. **It does not clear `messaging_blocked`:** an engineer who was blocked *and* deactivated comes back still blocked, and the gate returns `reactivate` (`reactivation.ts:36-37`), so they must text START themselves — BOT-27's principle (the flag is the engineer's opt-out, "NOT PM-clearable", `status.ts:103-110`) is not weakened by an admin action. **Naming collision, noted:** "reactivation" in `lib/whatsapp/reactivation.ts` is BOT-27's clearing of `messaging_blocked` for an *active* engineer (`:3-14`, `:29-40`); this function is the admin's un-deactivation, and its header states why it must be explicit: "a deactivated engineer must NEVER be silently reactivated by texting in" (`:13-14`).
**~~rev8: what happens to `deactivated_by`/`deactivated_at`~~ — superseded (rev9, §2.8).** rev8 showed that neither "clear" nor "keep the first record" preserves "past dates stay unchanged" and proposed `reactivated_by/at`. **Aravind chose episodes:** a reactivation **opens a new episode and touches no earlier one**, so every earlier interval — the gap included — stays exactly as recorded. rev8's stated limit ("one episode only; a second cycle overwrites the first") **no longer exists** and is struck.
**Reactivation day — the mirror of Edge 1, named and not built.** The rule is day-granular: the day an episode opens is an expected day. An engineer reactivated at 14:00 was not sent the 08:30 morning message (`roster.ts:168` excluded them), so a morning half with no submission would read amber after the cutoff for a check-in never sent. The exact fix is Edge 1's branch keyed on `istParts(opened_at).minutes` against `CHECKIN_CHECKPOINTS.morningSend`/`eveningSend` (`cutoffs.ts:49,62`) — the leave-side twin of `dispatch.ts:427`. **Not specified as required; D15 (ii)** for Aravind.
**The classifier offers it instead of a dead end (Edge 2).** A deactivated engineer **keeps their `engineer` membership** (§2.9), so re-pasting their number would classify `already_on_this_project` (R5) — the opposite of what the admin needs. **New outcome `deactivated_on_this_project`**, produced in step 5 only for a number that resolves, **inside the caller's tenant**, to a `users` row **with a membership on this project** whose `status = 'deactivated'`. Payload `{ idx, status, user_id }` — safe here (same tenant, same project; the caller can already see both through RLS, probe j) and what the flow passes to `reactivate_engineer`. An **active** same-project engineer stays R5 (`{idx, status}`); a same-tenant deactivated engineer on **another** project stays `on_another_project`; one with **no** membership stays `registered_no_project` (reassignment stays deferred); **cross-tenant stays the generic `number_registered`, exactly `{idx, status}`** — status is read only after the tenant/project scope has matched, so another tenant's user status is never observable (T5's payload rule unchanged). Preview shows the row as its own state (`DEACTIVATED_ON_THIS_PROJECT`) with a `REACTIVATE_CONTROL` calling `reactivate_engineer` (confirm → `REACTIVATE_CONFIRM`; result → `REACTIVATE_RESULT`; failure → `REACTIVATE_ERROR_NOT_FOUND`); an apply aborts while that row stands (any non-`ok` row, §2.3 step 6), so the admin reactivates, re-previews, sees R5 and removes the line.
**Deactivation takes effect with no further change, so reactivation needs none either** — `fetchActiveEngineers` filters `.eq('users.status','active')` (`roster.ts:168`) and `decideInboundGate` returns `proceed` for `status = 'active'` and `messaging_blocked = false` (`reactivation.ts:29-40`); both flip back on the one `status` UPDATE (T32).

### 2.11 Unreachability report — IN SLICE, a report and never a gate (Aravind, rev8; predicate revised, rev9)
**Why.** §4.6: the validator catches malformed numbers, not wrong ones, and a one-digit typo is a **live stranger's handset that receives the production templates and never replies**. Nothing today would notice. **What it is:** a **derived read** — no new table beyond §2.8. **What it is not:** it **never blocks, delays or alters an add** — it runs from a separate scheduled route after the fact, shares no code path with `add_engineers_to_project`, and a failure inside it is caught and reported, never raised into anything else (T37).
**The predicate** (per engineer): `users.role = 'engineer'` **and** `status = 'active'` **and** `registered_at IS NOT NULL` **and** `registered_at <= now() − N days` **and** at least one `outbound_sends` row with `recipient_user_id` = the engineer **and** `to_phone_number` = their current `whatsapp_number` (any `status`; the latest one's `status` and `error` are carried as context) **and** ~~rev8: no `daily_logs` row with a submitted timestamp~~ **rev9: NO `whatsapp_sessions` row whose `phone_number` equals `users.whatsapp_number`.** Legacy engineers have `registered_at` NULL and are out of scope by construction.
**Schema it reads (printed, log):** `outbound_sends` (`031:436-499`) — `id, created_at, updated_at, tenant_id, project_id, recipient_user_id, event_key, status ('sending'|'sent'|'failed'), content_sid, to_phone_number, twilio_sid, error`; **grants `service_role` only — `SELECT, INSERT, UPDATE` (`031:557-558`)**, so the report runs server-side on the service client and is **not** a PM-facing screen.
**"This number has never sent us anything" — every per-inbound record, examined (rev9).**
| Source | Per number? | Verdict |
|---|---|---|
| `processed_messages` (`011:8-13`) | **No** — `message_sid` and timestamps only; prunable (`011:19-22`); an unregistered or gated number leaves "ZERO storage footprint" (`route.ts:114-115`) | unusable |
| **`whatsapp_sessions`** (`001:86-97`) | **Yes** — `phone_number`, `user_id`, `updated_at`; one row per phone (both sides unique, `reachability.ts:26-38`) | **best available** |
| `daily_logs`, `daily_log_photos` (`ingest.ts:135`), `hindrance_photos` (`hindrance-ingest.ts:127`), hindrances | per engineer, but **downstream of a session** | strictly narrower |
| `outbound_sends` delivered / read | **not recorded** — `lib/whatsapp/outbound/status-callback.ts` header: delivered/read are a NO-OP; only `failed` is written (status-callback `route.ts:115-116`) | no live-handset signal |
| `users.messaging_blocked` | **no non-test code ever sets it `true`** (grep, log) | no opt-out signal |
**Why `whatsapp_sessions` is the truer signal.** A row appears only through the flow RPCs (`INSERT INTO whatsapp_sessions` at `012:99`, `014:70`, `022:131,384`, `024:324`, `025:202`, `030:362`, `035:259,535`, `038:251,443,769`), and those are called only from the webhook path — `applyMorningFlowTurn` (`webhook/route.ts:7,300`), `routeInboundMessage` (`:8,324`) and the flow modules it calls; **the send path never touches the table** (grep: zero hits for `whatsapp_sessions`, `acquireAndTransition` or the flow RPCs under `lib/whatsapp/outbound/` and `app/api/cron/`); nothing deletes rows (grep s4). So a row means **an inbound from that number reached the flow layer at least once** — and a **hindrance-only replier is no longer a false positive** (the hindrance RPC inserts a session, `038:251`), which was the rev8 defect.
**Residual classes, recorded explicitly — this is the best signal available, not a perfect one.**
- **FP-1 (still flagged though they did write):** an inbound that received only the static "no active session" acknowledgement (`design-decisions/check-in-architecture-and-triggers.md` §38) may leave no session row; whether a bare first message creates one depends on the flag-gated branch at `webhook/route.ts:290-300`, which I did not resolve (UNKNOWNS #29).
- **FN-1 (not flagged though wrong):** a **stranger at a mistyped number who replies at all** — even "wrong number" — creates a session and hides the typo. The report catches **silent** strangers, the common case, not all.
- **FN-2 (fragility):** the predicate is true only while **the send path never writes sessions**. `SessionCaller` already reserves `'scheduled_trigger'` (`session.ts:10`) for a cron-started flow, with **zero call sites today** (grep). The day a trigger calls `acquireAndTransition`, "a session exists" would mean "we sent", and the report would silently stop reporting. **T37 (iv) is a source guard** on exactly today's grep.
**Follows the existing Sentry dedup shape** (`roster.ts:132-148`, "same convention as `reportMorningSweepAnomalies`"): `Sentry.captureMessage(<developer-facing title in the `'<feature>: <what happened>'` shape the two existing reporters use — written in the build slice, not drafted here>, { level: 'warning', fingerprint: ['engineer-registration', 'no-inbound-since-registration', <engineerId>, <IST date>], tags: { feature: 'engineer-registration', reason: 'no_inbound_since_registration' }, extra: { engineer_id, project_id, registered_at, sends: <count>, last_send_status, last_send_error, log_date } })` — one growing issue per engineer per day. **No phone number in the payload.**
**Where it runs:** a new daily route (`app/api/cron/engineer-reachability-report/route.ts`, ASSUMED name) behind `CRON_SECRET`, with one new `vercel.json` cron entry — **merging it is a deploy** (`vercel.json:1-24`). **ASSUMED time:** 05:00 UTC (10:30 IST), after the 08:30 IST send and its retries; not inside `jobs/tick` (every minute) nor the morning-trigger route (critical send path).
**N — the threshold — is a DECISION FOR ARAVIND. Recommend N = 3 days.** Two sends a day (`vercel.json:12-19`) means ≥ 6 unanswered sends by day 3; a weekend or a one-day closure does not trip it. N is one named constant (`lib/engineers/unreachable-report.ts`, ASSUMED). The join is by value on two unique indexes (`reachability.ts:26-38`); no index proposed, none measured (UNKNOWNS #30). **Tests** T36–T38 (§4.9).

## 4. Rejection cases — row R5b only (the rest of §4 is in slice 1)

| # | Reason | Detected by | Preview? | Apply? | Shown by |
|---|---|---|---|---|---|
| R5b | **Deactivated** engineer already on **this** project (rev8) | `deactivated_on_this_project` + `user_id` | yes | yes | `DEACTIVATED_ON_THIS_PROJECT`; the flow offers `REACTIVATE_CONTROL` (§2.10) — not a dead end |

### 4.8 (the D13 part; the "ships without" and "future slice" part is in slice 1)

**D13 — F4 options (rev6) — SUPERSEDED by §4.9.** ~~OPEN; not chosen; options 1–4 (do nothing / hide by `users.status` / hide date-aware by `deactivated_at` / explicit "Deactivated" state)~~ — Aravind chose the date-aware rule plus a visible marker (rev7 #1). The rev6 text, including the option table and costs, is `git show eff12a0:docs/plans/add-engineer-plan.md` §4.8. Its two findings stand and are used in §4.9: `users.status` is a current-state flag, not a per-day fact; and the board's roster-driven shape means a log with no roster entry is not displayed (`query.ts:226-260`).
(Related, pre-existing, same root cause: because the roster is current-state, a **newly added** engineer also shows "Not checked in" on past dates before they joined — relevant to how the add screen will look; `status.ts:130-137`.)

### 4.9 D13 SETTLED — the board rule, restated on episodes (rev9)
**Decision (Aravind, 19 Sep 2026):** in this slice; dated; **not** a status filter. A deactivated engineer stops being expected from the deactivation date onward — today's board, the Today "Needs your attention" list and its heading count, every later date — and **history is not erased**: every earlier date, and every log the engineer submitted, is unchanged. *(rev7 keyed this on `users.deactivated_at`; rev8 added `reactivated_at`; **rev9 keys it on episodes, §2.8** — struck below where it mattered.)*
**A plain `WHERE users.status = 'active'` is WRONG here.** `users.status` is *current state*. The board iterates the roster **per date** and shows a log only through a roster entry (`query.ts:226-260`), so a status filter applied while the PM looks at last Tuesday drops last Tuesday's card and the log submitted that day. The boundary must be a **date**, and the only per-date facts are the episodes. (`status` is right in exactly one place — the project-detail team list, which has no date dimension.)

**The rule, against episodes.** For a roster entry (engineer E on project P) and a board date L (IST `'YYYY-MM-DD'`), with `eps` = E's episodes on P:
1. **`eps` empty → expected** (a legacy engineer or a directly-inserted membership; behaviour unchanged).
2. Otherwise **expected ⟺ some episode has `istDate(opened_at) ≤ L` and (`closed_at` NULL or `L < istDate(closed_at)`)** — equivalently, E was in an open episode **at the end of IST day L** (`opened_at < start of IST day L+1` and (`closed_at` NULL or `closed_at ≥` that instant)).
- **Which episode bounds a date:** at most one — the open one and closed ones do not overlap (§2.8) — the one whose IST-day span `[istDate(opened_at), istDate(closed_at))` contains L. **The opening day is included; the closing day is excluded** (so the deactivation day D is excluded, as D13 says, and a reactivation day R is expected). An episode opened and closed on the same IST day is an empty span.
- **A date inside a gap between episodes is not expected.** So is a date before the first episode and a date after the last close. Not expected + no log ⇒ **the entry is omitted**. Not expected + a log exists ⇒ **the card is kept, `afterDeactivation: true`** (D14 i). *(The field keeps its rev7 name; it now means "this date lies outside every episode".)*
- ~~rev7/rev8: `isAfterDeactivation(status, deactivatedAt, reactivatedAt, logDate)`, "`deactivated_at` NULL ⇒ legacy", and the `status`-guard bullet~~ — **struck**: with no `deactivated_*`/`reactivated_*` columns there is nothing to guard. `pending` users are not touched (no episode).
- **IST, not UTC.** Executed on the real helpers (rev7 log, `ist-boundary.mjs`, re-run in rev9): an instant `2026-09-19T18:30:00+00:00` has IST date `2026-09-20` (UTC slice `2026-09-19`); `istDateString` (`date.ts:27-36`) and `istParts(…).date` (`status.ts:47-61`) agree; the Postgres microsecond form parses. Comparing zero-padded date strings is chronological and is already how `query.ts:622` orders dates. A UTC slice mis-dates every open/close in 18:30–24:00 UTC (T23).
- **Consequence to flag (D18):** for an engineer **with** episodes, dates before their first episode are not expected — so an engineer added today no longer reads amber "Not checked in" on yesterday's board (the wart noted in §4.8: "a newly added engineer also shows amber on past dates before they joined"). This is the leave-side twin of the join-side rule already in the DPR (`dispatch.ts:427-428`, `joined_late`). Legacy engineers (no episodes) are unchanged. **ACCEPTED (Aravind, rev10).** **The inconsistency, named:** under **lazy materialisation** (§2.8), engineers WITH episodes get this join-side rule and engineers WITHOUT episodes do not, so **the board behaves two ways** until every engineer has an episode — which lazy materialisation only does on a first deactivate/reactivate (no eager backfill is planned; engineers added by slice 1 have none).
- **"Any future date":** the Daily Logs page clamps to today (`daily-logs/page.tsx:40-41`) and Today reads today only (`dashboard/page.tsx:116`); `getDailyLogsBoard` itself accepts any `logDate` (T24a uses "tomorrow").

**Per roster entry and board date L** (`expected = isExpectedOn(eps, L)`):
| `expected` | `daily_logs` row for (P, E, L) | Result |
|---|---|---|
| true | any | **Unchanged**: card, log, chips exactly as today |
| false | none | **Entry omitted** |
| false | exists | **Card kept, `afterDeactivation: true`** — the log stays visible; no expectation is generated from it (chips: Edge 1) |

**What answering it costs — the query shape (rev9 item 2, `query.ts` printed in full, log).**
**Today:** `getDailyLogsBoard` (`:133-267`) runs the PM-scope read (`:141-144`), then in one `Promise.all` the **roster** (`project_members` `.in('project_id')` `.eq('role','engineer')` with an embedded `users(...)`, `:159-163`) and the **logs** for the date (`:164-170`), merges by `${project_id}:${engineer_id}` (`:180-181`) and builds cards by iterating the roster (`:226-260`).
**With episodes:** **one more member of the same `Promise.all`** — `engineer_episodes` `.select('project_id, user_id, opened_at, closed_at').in('project_id', projectIds)` — merged in TypeScript into a `Map` keyed `${project_id}:${user_id}`, exactly like `logByKey`, and evaluated with `istDateString` string comparisons. **No date predicate in SQL**, deliberately: rule 1 must tell "no episodes (legacy)" from "a gap", and a date-bounded read cannot. **No join** is added; the roster select, its row count and its response shape are **unchanged**.
**Rows and cost:** the episodes read returns Σ episodes of engineers on the PM's projects = roster rows × the average episodes per engineer-project — **1 for an engineer only ever added, +1 per deactivate/reactivate cycle** (each a human admin act). It is **not per date and not per log**, runs in parallel with the two existing reads, and uses `idx_engineer_episodes_project (project_id, opened_at)` (§2.8; `project_id = ANY(…)`); a second index `(user_id, opened_at)` serves the FK and the detail lookup. `getDailyLogDetail` gains one indexed read `(project_id, user_id)`. **So: it can be answered without a materially more expensive per-date query and without a join the current shape cannot carry — this is NOT a blocking finding.**
**Rejected shapes, with reasons:** (a) embedding `engineer_episodes` under the roster's `users(...)` — multiplies the payload by every project the engineer ever had, changes the roster response shape, and needs a two-level filter on the embed (`referencedTable: 'users.engineer_episodes'`) that I did not verify; (b) a date-bounded "stabbing" read in SQL — fewer rows, but cannot separate legacy from a gap, so it needs a second existence read anyway; (c) a per-date RPC — a new function surface for no gain.
**Honest costs, not smoothed:** (i) **two records must agree** (`users.status` for rosters, episodes for the board) — invariant I1 (§2.8), T34, and probe `e1` (§11); (ii) a **fifth board read** on the detail page and a **fourth** on the list pages (memberships, roster, logs, episodes; + photos when asked); (iii) the new table is read under a tenant-wide RLS policy, so a PM's JWT can read episodes of any project in the tenant — same reach as `project_members_select` (`002:114-116`), bounded to the PM's projects only by the app-layer `.in('project_id')`.

**What changes, where (specification only; nothing is edited in this pass).**
| File | Function / place | Change |
|---|---|---|
| `lib/daily-logs/query.ts` | `getDailyLogsBoard` `:158-171` | add the episodes read to the `Promise.all`; a failed episodes read returns `{ status: 'error' }` via `reportReadFailure` like the other reads (`:173-174`) — **never** "everyone expected" (the B1 rule, `:75-89`) |
| same | roster loop `:226-260` — **the only predicate that changes** | after `log` resolves (`:229`): `expected = isExpectedOn(eps, logDate)`; `if (!expected && !log) continue`; push `afterDeactivation: !expected` |
| same | `EngineerCard` `:21-67`; new exports | required field `afterDeactivation: boolean`; pure **`isExpectedOn(eps, logDate)`**; pure **`collectMissingCheckins`** (the Today loop `dashboard/page.tsx:140-174` moved verbatim, passing `e.afterDeactivation` into both `deriveHalfStatus` calls — the rev7 separate `continue` is struck) |
| same | `getDailyLogDetail` `:368-474` | one read of the (project, engineer) episodes; return `afterDeactivation` for the log's own `log_date` |
| unchanged | `.eq('role','engineer')` `:163`; PM scope `:141-144`; logs read `:164-171`; `getNeededTomorrowItems` `:549-627` | none |
| `lib/daily-logs/status.ts` | `deriveHalfStatus` `:68-76`; `HalfState` `:20` | **Edge 1:** optional 7th parameter `notExpected`, one branch after "submitted", new state `not_expected` (label `''`, no chip) |
| `app/(dashboard)/dashboard/page.tsx` | loop `:140-174` | replaced by `collectMissingCheckins`; chip text (`:159`, `:170`) and title (`:83-87`) unchanged |
| `app/(dashboard)/daily-logs/engineer-card.tsx` | `:109-110`, `:89-96`, name line `:119-121` | pass `eng.afterDeactivation`; `HalfRow` renders nothing for `not_expected`; marker `ENGINEER_STATUS_DEACTIVATED` with `data-engineer-status="deactivated"` when `afterDeactivation` |
| `app/(dashboard)/daily-logs/page.tsx` | `:69,71` | pass the flag (degraded-CTA pre-scan) |
| `components/daily-logs/log-detail-view.tsx` | `:431,439` | pass `data.afterDeactivation` (same chips on the detail page; **rev7 missed this site**) |
| `app/(dashboard)/projects/[id]/page.tsx` | select `:57`, `UserRow` `:16-20`, row `:129-134` | add `status` to `users(id, full_name, role)`; extract the row to a presentational `team-member-row.tsx`; the same marker when `u.status === 'deactivated'` — **status-based, correct here** (current state, no date). The membership still exists, so the row stays |

**What each surface shows the day after** (D = deactivation IST date; the PM looks on D+1):
| Surface | On D+1 | On D-3 | On D itself |
|---|---|---|---|
| **Daily Logs** | No card for them in that project's section; others unchanged. If they were the only engineer: the existing "No engineers assigned to this project." (`daily-logs/page.tsx:131`); if no project has any, "No engineers assigned yet." (`:110-113`) | **Unchanged**: card, submitted log, and any amber "Not checked in" exactly as today | No card **unless a log exists**; then the card, marker, no expectation chip |
| **Today** | Absent from the missing-check-in loop: no "hasn't sent a … check-in" card, **no contribution to the heading count** from those terms | not shown | skipped even when a log exists |
| **Project detail** | Row stays (membership exists) with the marker — not date-aware, by design | same | same |
**Heading count, exactly.** `grandTotal = hindranceItems + missingCheckins + neededTomorrowItems` (`dashboard/page.tsx:176,184`). Deactivation removes only the **missing-check-in** term. The **"Needed tomorrow"** term reads `daily_logs` rows in a two-day window, roster-independent (`query.ts:572-577`): a dependency reported on the evening of D still shows on D+1 and is still counted — **D14 (ii)**.

**Edge 1 — the kept card's chips.** ~~rev7: `deriveHalfStatus` unchanged.~~ Unchanged, a kept card makes a **false statement**: an engineer deactivated at 15:00 after a 09:00 check-in shows "Awaiting evening" and then an amber **"Not checked in" from 19:15 IST** (`status.ts:130-153`; `cutoffs.ts:64,89-90`). **Printed:** `deriveHalfStatus(log, messagingBlocked, half, logDate, now, cutoffs)` (`status.ts:68-76`): submitted (`:80-88`), holiday (`:90-99`), `messaging_blocked` today-only (`:103-123`), the clock (`:125-153`). **Call sites:** `engineer-card.tsx:109-110`; `daily-logs/page.tsx:69,71`; `dashboard/page.tsx:148-149`; **`log-detail-view.tsx:431,439`**; `test/unit/daily-logs-status.test.ts` (14 calls, six positional arguments). **Specification:** an **optional 7th parameter `notExpected = false`** (every existing call, tests included, is unaffected) and **one branch immediately after "submitted"**: `if (notExpected) return { state: 'not_expected', variant: 'muted', label: '', countsForAccountability: false }`. **Submitted wins.** `HalfState` (`status.ts:20`, its only definition; no exhaustive switch elsewhere — grep, rev8 log) gains `'not_expected'`. **The label is empty and the card renders no chip** for it; `ENGINEER_STATUS_DEACTIVATED` is the only wording — **no string is invented.** Asked for: the evening half; **the same branch also covers a morning half with no submission** — flagged **D14 (iii)**.

**Deploy order — MERGE is the deploy: the rule, the five-step order, the rollback order and the Preview-database finding are in slice 1 §6.1 (moved there verbatim in rev10 — nothing was dropped; UNKNOWNS #33 and #34 are in both documents).** **What is specific to slice 2:**
**The board now reads `users.status` (existing) and `engineer_episodes` (new).** Against a database without the table PostgREST rejects the read; the board returns `{ status: 'error' }` (`query.ts:173`) and **both** Daily Logs (`daily-logs/page.tsx:101-102`) and Today (`dashboard/page.tsx:128-130`) render their error state (the mechanism of rev7 probe pg_7, same class).
**Slice 2 applies the same order to its own migration:** apply it to test-db, CI green on slice 2's PR (pinned run URL, `headSha` = PR HEAD), apply to prod, **then** merge; rollback is **revert commit first, then the DOWN** — and this DOWN **destroys the episode history** (§6). It applies **after** slice 1 has shipped.

**Could a component I did not open list engineers from `project_members` and bypass the rule?** Ruled out **as far as static and test-db evidence goes** (rev8, unchanged): every `project_members` reader in `app`/`lib`/`components` is enumerated (grep `b2`, the full-tree grep); roster-shaped ones are `query.ts:159`, the outbound/escalation/DPR rosters (all filter `users.status`), `projects/[id]/page.tsx:56`; the rest read the PM's own membership or one pair; no file under `components/` contains the string; no `.from(` takes a non-literal table; no view mentions it (probe pg_1); two functions do (probe pg_2), neither returns a roster to a screen. **Not ruled out:** prod-only objects and unread test files — UNKNOWNS #21.

**Tests (named; each states how it is shown to FAIL first).** Board tests run on the real test-db, need 048 applied there, use `+1…`-shaped or NULL numbers (**no `+91` value is minted**, T21 stays true), and clean up. **Episodes are only ever written by the three definer functions** (`service_role` holds `SELECT` only), so integration tests create episodes **by calling the real functions**, at real `now()`; ~~**the multi-day gap semantics are proven in the pure T23**, where the whole interval logic lives. A back-dating seed helper would be a test-only object (precedent `scripts/test-db-only-grants.sql`, which `CLAUDE.md` §0 requires to be *named* as an exception) — **not specified; D20.**~~ **rev10 (D20 changed): the multi-day gap semantics are proven in the pure T23, but gap semantics proven in a pure test do NOT prove the board query — a pure test cannot go red for a query bug. T43 (below) is the database-level test that does.** It needs episodes with **explicit past timestamps**, which the real functions cannot write; the seeding mechanism is flagged in UNKNOWNS #50. Legacy fixtures are `project_members` rows inserted by the service role with an explicit `created_at`.
| # | Asserts | How it is shown to FAIL first |
|---|---|---|
| **T23** | **`isExpectedOn(eps, L)` truth table** (pure): empty → expected on every date; one open episode from D0 → `L ≥ D0` expected, earlier not; closed `[D0, D1)` → D1 excluded, D1−1 expected; **IST edges** (`closed_at = 18:29:59Z` → its own IST day, `18:30:00Z` → the next); **a gap `[D1, D2)` excluded, D2 expected** (opening day included); an episode opened and closed on one IST day → never expected; two episodes; a date before the first episode → not expected; microsecond-form input parses. | One mutation per row: UTC `slice(0,10)`; `<=` for the closing day; "empty ⇒ not expected" (the legacy row); the hull `first-open … last-close` instead of the union (the gap row); `<` for the opening day. |
| **T24a** | **An engineer deactivated on D is absent from D+1's board.** Legacy engineer E (membership `created_at` = D−10) and a control C; `deactivate_engineer` as the PM; `getDailyLogsBoard(tomorrow)` has C's card and **no** card for E. | **Natural red:** today's function has no predicate — E's card is present. |
| **T24b** | **A log E submitted on D−3 is still visible and unchanged.** A full `daily_logs` row for E at D−3 (both halves, texts, dependency); after `deactivate_engineer`, `getDailyLogsBoard(D−3)` returns E's card, `log.id` equal, `morning_submitted_at`, `evening_submitted_at`, `morning_plan`, `evening_output`, `evening_tomorrow_needs` **deep-equal to a direct `daily_logs` read**, `afterDeactivation === false` (the materialised episode starts at D−10). Plus a **missed** D−4: card present, `log` null. | **Red only under the wrong rule — that is its job.** Against today's code T24b already passes. It fails under the mutation this plan rejects: `users!inner(...)` + `.eq('users.status','active')` → E's D−3 card vanishes, while T24a still passes; the pair separates the right rule from the wrong one. |
| **T24d** | **The deactivation day.** E has a log dated today, then is deactivated: `getDailyLogsBoard(today)` keeps E's card with `afterDeactivation === true`; E2 (no log) → absent. | "Drop whenever not expected" hides the log → fails; "always keep" shows E2 → fails. |
| **T24e** | **Legacy.** No episodes → every date unchanged, `afterDeactivation === false`. | Mutation: treat "no episodes" as not expected → cards vanish → fails. |
| **T24f** | **A failed episodes read is an error, not "everyone expected."** A client whose `engineer_episodes` read errors → `{ status: 'error' }`. | Mutation: ignore the error → `ok` with everyone expected → fails. |
| **T25** | **`collectMissingCheckins` (pure):** E (`afterDeactivation: true`, no log, today, past 19:15 IST) and a control (`false`) → rows for the control, **none** for E, via the `deriveHalfStatus` branch; positive control — E with `false` **does** produce rows. | Mutation: pass `false` at the call sites → rows appear → fails. |
| **T26** | **Composition on real data:** the board from T24a fed to `collectMissingCheckins` at a `now` after 19:15 yields exactly the control's rows. | Natural red against today's code. |
| **T27** | **Card marker** (`renderToStaticMarkup`, the two `vi.mock`s and `await import` of `test/daily-log-card-photo-gate-render.test.ts:1-70`): `afterDeactivation: true` contains `data-engineer-status="deactivated"`, `false` does not. **Keyed on the attribute — every string is blank (§9).** | Natural red: marker absent. |
| **T28** | **Project-detail row:** `TeamMemberRow` with `status: 'deactivated'` contains the attribute **and the member's name**; `'active'` and `'pending'` do not. | Natural red; mutation `status !== 'active'` → the `'pending'` case fails. |
| **T29** | **Deploy-order regression:** after 048, the existing board tests that call this module stay green. | **Natural red, and the evidence for the hazard:** the new `query.ts` against test-db **before** 048 → every board call errors. |
| **T30a** | **`deriveHalfStatus` — `notExpected`** (pure): evening, today, after 19:15, blank, `true` → `not_expected`, `label === ''`; control `false` → `missing`; **submitted + `true` → `submitted`**. | Natural red: the 7th argument is ignored → `missing`. Mutation: branch before "submitted" → the submitted case fails. |
| **T30b** | **Kept card, no false chip** (render): morning submitted 09:00, `now` = 19:30 IST → **no** `Not checked in` (an existing real string), the marker present; control card → `Not checked in` present. | Natural red. |
| **T30c** | **Detail page:** `LogDetailView` with `afterDeactivation: true`, after 19:15 → no `Not checked in`; control present. | Natural red: `:431,439` do not pass the flag. |
| **T31** | **`reactivate_engineer` gate and tenant.** Tenant-A admin cannot reactivate a tenant-B user: `no_data_found`; read-back shows B's user **unchanged and no episode opened**. `pm` without a membership, and `qs`/NULL role with a `pm` membership → `insufficient_privilege`. The shared T6 matrix runs against it too. | Drop the tenant binding → B's user is reactivated → fails. Skip the role gate → the `qs` row reactivates → fails. |
| **T32** | **A reactivated engineer is live again.** Positive control: deactivated → **absent** from the outbound roster (`roster.ts:195-205`, `:168`) and `decideInboundGate` = `gated_noop`. After reactivate: **present**, `proceed`. A `messaging_blocked` + deactivated engineer → after reactivate the gate is **`reactivate`, not `proceed`**. | Function absent → still excluded. Mutation: also clear `messaging_blocked` → `proceed` → fails. Mutation: `status = 'pending'` → `gated_noop` → fails. |
| **T33** | **Reactivation on the board (real functions):** E deactivated then reactivated; two episodes exist (first `closed_via 'deactivated'`, second open `'reactivated'`); `getDailyLogsBoard(today)` and `(tomorrow)` show E (the reopening day is expected); a log seeded at D−3 stays unchanged. **The multi-day gap** is T23's gap row. | Natural red against a status-only rule: none of the gap semantics exist. Mutation: reactivate writes `users` only (no episode) → `tomorrow` omits E → fails. |
| **T34** | **Episode lifecycle and I1.** Add → **one open** episode (`opened_via 'added'`, `opened_by` = caller's `users.id`, `opened_at` = `users.registered_at`). Deactivate → that episode closed (`closed_by`, `closed_via 'deactivated'`), `status 'deactivated'`, none open. Deactivate again → `already_deactivated`, no writes. Reactivate → a new open episode `'reactivated'`, the old one **untouched**. Reactivate again → `already_active`, still **exactly one** open. Legacy: first deactivate materialises a `'backfill'` episode from `project_members.created_at`. **I1 asserted after every step.** | Mutations: skip the close; skip the open; write `users` only; overwrite the old episode → each fails. |
| **T35** | **The classifier offers reactivation.** A same-tenant, same-project **deactivated** engineer's number, dry-run → `deactivated_on_this_project` with `user_id` = that engineer's id and **not** `already_on_this_project`; an **active** one → still `already_on_this_project`; a **cross-tenant** deactivated engineer → generic `number_registered`, **exactly `{idx, status}`**, no `user_id`; deactivated on **another** project → `on_another_project`. | Natural red: today's classifier returns R5 for the deactivated one. Mutation: read `status` before the tenant/project scope → cross-tenant `user_id` leaks → fails. |
| **T40** | **At most one open (disposable scaffold, owner role):** a second open episode for one user → `23505` naming `uq_engineer_episodes_one_open`; closed episodes may coexist; `closed_at < opened_at` → `23514`. | Without the partial unique index the second open succeeds → fails. |
| **T41** | **Grants and RLS on the REAL test-db:** `anon` `SELECT` → `42501`; `authenticated` `INSERT`/`UPDATE`/`DELETE` denied; a **tenant-B** PM reads **zero** tenant-A episodes (two-tenant fixture); `service_role` `INSERT`/`UPDATE`/`DELETE`/`TRUNCATE` denied (`CLAUDE.md` §7 rehearsal requirement). T16 also covers all four functions incl. an `anon` call to `reactivate_engineer` → `42501`. | Before the REVOKE, `service_role` and PUBLIC hold the default ACL (probe n). |
| **T42** | **Concurrent reactivates / deactivates serialise** — exactly one wins; the index is the backstop. | **NOT VERIFIED LOCALLY, CI-ONLY** (`CLAUDE.md` §0). |
| **T43** | **The board query excludes a gap date — database level (D20, rev10).** Seed, with explicit past timestamps (mechanism: UNKNOWNS #50), engineer E on project P with **two episodes and a gap between them** — `[D−10, D−6)` closed and `[D−2, ∞)` open — a control engineer C with no episodes, and a real PM. `getDailyLogsBoard(G)` with **G = D−4 (inside the gap)**: E's card is **absent**, C's present. The same call with a `daily_logs` row seeded for E at G: E's card is **kept, `afterDeactivation === true`**. `getDailyLogsBoard(D−8)` and `(D−1)`: E's card **present, `afterDeactivation === false`**. | **Shown failing first, three ways — each a QUERY bug the pure T23 cannot see:** (1) **natural red** against today's `getDailyLogsBoard` (no episodes read) — E is present at G; (2) mutation: the episodes read is **filtered to open episodes only** (`closed_at IS NULL`) — E's first episode vanishes and D−8 reads not-expected → fails; (3) mutation: the episodes read is **keyed on the wrong column / drops `.in('project_id', …)`**, so E has no episodes and reads as legacy — E is present at G → fails. T23 stays green under all three because it feeds the rule hand-built arrays. |
| **T36** | **Unreachability classifier** (pure), N = 3: flagged — `registered_at` N days ago, ≥1 send to the current number, **no session row**; **not** flagged — N−1 days; a **session row exists** (including a hindrance-only replier); **zero sends**; `deactivated`; `registered_at` NULL. | One mutation per row: `>` for `>=`; drop the session clause; drop the send clause; drop the status clause; count `daily_logs` instead of sessions (the rev8 defect — the hindrance-only row fails). |
| **T37** | **Never a gate, and the fragility guard.** (i) no file under `app/(dashboard)/projects/[id]/engineers/` or `lib/engineers/{add-engineers,deactivate,reactivate}.ts` imports the report; (ii) the route catches a thrown read, reports to Sentry, returns without raising; (iii) the fingerprint is exactly `['engineer-registration','no-inbound-since-registration', engineerId, istDate]` and `extra` has **no phone number**; **(iv) source guard: no file under `lib/whatsapp/outbound/` or `app/api/cron/` references `whatsapp_sessions`, `acquireAndTransition` or a flow RPC** (FN-2). | Import the report from the add path → (i) fails; remove the try/catch → (ii); add `to_phone_number` to `extra` → (iii); make a trigger call `acquireAndTransition` → (iv). |
| **T38** | **Integration on test-db** (`+1…` numbers, no real send): E1 registered N+1 days ago with a ledger row and no session → flagged; E2 the same with a `whatsapp_sessions` row → not; E3 registered N−1 days ago → not. `registered_at` set by the service role; `outbound_sends` rows inserted directly (`service_role` holds `INSERT`, `031:558`). | Natural red: module absent. |

**D14 — collisions between D13's rule and "history stays" — a FLAGGED DEFAULT, applied, not silently chosen.** (i) **The deactivation day, when the engineer already submitted:** literal exclusion hides that log from D's board for good; default: keep the card, marker, no expectation; alternative: drop whenever not expected (delete the `!log` clause). (ii) **"Needed tomorrow" items** from their logs stay shown and counted; alternative: filter `getNeededTomorrowItems` by the same rule. (iii) **The Edge 1 branch also covers a morning half.** Not blocking; each is a one-line change. **Also named, not chosen:** a project whose only engineers are all deactivated shows the existing "No engineers assigned…" sentence — a wording decision for Aravind.

## 6. The migration (slice 2)

- **Migration number (consequence of the split):** slice 1 carries `048`; slice 2's number is whichever is free when it is drafted (**ASSUMED `049`**), reserved then in `scripts/migration-number-reservations.json` (`CLAUDE.md` §0). It is held in `docs/reviews/` until applied and **applies after slice 1's**.
- **What slice 2's migration contains (from the single-plan list at `eb8a9c2`, items (2b), (5), (5b), (6)):** (2b) **the `engineer_episodes` table** with its CHECKs, composite same-tenant FKs, three indexes (incl. the partial unique index that enforces one open episode), RLS and explicit grants (§2.8); (5) `deactivate_engineer`, (5b) `reactivate_engineer` (§2.10), (6) their ACLs (§2.7 of slice 1) — **plus, as a consequence of the split, the redefinition of slice 1's `add_engineers_to_project` so the add path opens an episode** (see the "Consequences of the split" note at the top). **The reviewer signed off on the five-column design; (2b) and the episode writes in (4)/(5)/(5b) are new since then and need the full package.**
- **Review gate (`CLAUDE.md:192-205`) clearly tripped** (a, b, c; a `DROP COLUMN` in the DOWN is destructive (d)). Required evidence: anon-key call refused `42501` (`CLAUDE.md:934-942`); `service_role` denial on the **real** database; ACL / `proowner` / `proconfig` for **all four** functions (helper + `add` + `deactivate` + `reactivate`); disposable local dry-run first (`CLAUDE.md:1020-1030`); rehearsal on the cleaned test-db (`CLAUDE.md:74-78`).
- **DOWN (rev9):** also drops `engineer_episodes` — **it destroys the whole episode history**, and (rev8) the board reads that table, so the app change must be reverted and deployed first (§4.9). The `deactivate`/`reactivate` semantics are lost with it; a DOWN after real use is a data-loss event, to be stated as such in the runbook.
- **Deploy order (rev8, §4.9): MERGE is the deploy.** 048 → **test-db** → CI green (pinned run URL, `headSha` = PR HEAD) → 048 → **prod** → **THEN merge**; rollback = **revert commit (merged) first, then the DOWN**. ~~rev7: "048 lands on test-db and prod BEFORE the app change deploys"~~ — the event is the merge, not a deploy step this repo can show.
- **After apply:** regenerate `types/database.ts` (`CLAUDE.md:853-858`); one file at a time via `supabase db query --linked -f`, foreground, never `db push` (`CLAUDE.md:156-160`); confirm the file is on `origin/main` and test-db carries it (`CLAUDE.md:142-146`).

## 7. Positive controls (slice 2 rows)
"Shown to fail" = a captured red run, then the fix, then green. Red variants of the function run on the **disposable local scaffold** (`CLAUDE.md:1020-1030`), never on test-db or prod.

### 7.1 Tests (rows of slice 2; T1–T22 are in slice 1, T23–T43 are specified in §4.9)

| # | Asserts | How it is shown to FAIL first |
|---|---|---|
| **T10b** | **The add path opens the episode.** After apply: one open episode per added engineer — `opened_via = 'added'`, `opened_by` = the caller's `users.id`, `project_id`/`tenant_id` as derived, **`opened_at` equal to `users.registered_at`** (§2.8's one duplication). Dry-run writes none. | Mutations: skip the episode insert; write `now()` separately (drift) → the equality fails; dry-run falls through → a row appears. |
| **T11** | **Deactivate.** (i) Tenant-A admin cannot deactivate a tenant-B user (`no_data_found`), zero writes. (ii) `pm` without membership, and `qs`/NULL role with a membership: `insufficient_privilege`. (iii) Positive control: the engineer **is** in `fetchMorningRoster`, gate `proceed`; after deactivation **excluded** (`roster.ts:150,203`) and `gated_noop` (`reactivation.ts:29-33`); number and membership untouched. (iv) ~~`deactivated_by`/`deactivated_at` set~~ the engineer's open episode is **closed** (`closed_by` = caller, `closed_via = 'deactivated'`); a **second call returns `already_deactivated` and writes nothing** (full lifecycle: T34). | (i) drop tenant binding → fails; (iii) function absent → fails; set `messaging_blocked` instead → gate returns `reactivate` → fails; (iv) always-write mutation → `closed_at` moves → fails; close without updating `users` (or the reverse) → I1 fails. |
| **T12** | **Copy is filled.** Every export of `lib/engineers/copy.ts` is non-empty. | **Expected-fail at commit** (`it.fails`): blank → assertion fails → test passes; when the copy PR fills them the wrapper goes red. **Verified** on vitest 3.2.7 (scratch dir, re-run in this log): blank → PASS, filled → FAIL, control PASS. No live `it.fails` exists in `test/` (grep f2). |
| **T16** | **ACL evidence** on the real test-db for **all four** functions (rev8: ~~three~~ — + `reactivate_engineer`): anon → `42501`; `service_role` denied; `authenticated` may call the three public functions and **not** the helper; `has_function_privilege` for `anon`, `authenticated`, `service_role`, PUBLIC; `proowner = postgres`; `proconfig` = `search_path=public`. | Before the REVOKEs, `service_role`/PUBLIC hold EXECUTE (probe n, `correct_daily_log`). |
| **T23–T29** | **D13 — the dated deactivation rule, the Today count, the two markers, and the deploy-order regression.** Specified in full, each with its red mechanism, in **§4.9** (T24a and T24b are the two the rule was written around). | §4.9 |

## 8. RLS (slice 2 — the full single-plan paragraph, plus the new table)

Live (probes j, k, o): `users` — `users_select` (own or same tenant), `users_update` (own row; column grant `full_name, avatar_url`, `015:105`); no INSERT/DELETE policy, `authenticated` lacks both (`015:114`). `project_members` — select tenant-scoped; insert/update require tenant match AND `users.role IN ('pm','admin')` **with no condition on the inserted row's `role`** (§4.5); no DELETE policy (`047:230`). Both tables owned by `postgres`, RLS on, **not forced**. **The four functions (helper + three public) bypass RLS**; the gate is re-stated inside (§1). Still enforced regardless of RLS: composite FKs (`017:94-106`), `UNIQUE (whatsapp_number)`, `UNIQUE (project_id, user_id)` (`001:79`), `users_role_check`, `users_status_check`, the new CHECK, the pairing CHECKs. Cross-tenant: tenant is never an input (§2.4).
**The `engineer_episodes` table (§2.8):** RLS enabled; one `SELECT` policy scoped by `tenant_id = get_user_tenant_id()`; **no** INSERT/UPDATE/DELETE policy; explicit `REVOKE`/`GRANT` naming `service_role` (`SELECT` only) — stated in §2.8 and proved on the real database by T41.

## 9. Strings (slice 2) — every value blank; Aravind writes all wording

~~D13's Option 4 would add **one blank constant** (a "Deactivated" state label) *if chosen* — not added now.~~ **rev7 adds none:** D13 needs exactly one label, **`ENGINEER_STATUS_DEACTIVATED`, already in the list below**, used in two places (the Daily Logs card marker and the project-detail row, §4.9); `ENGINEER_STATUS_ACTIVE` is not used by D13. Its value stays blank with `// Wording owed, NOT approved`; **T12** covers it. The tests key on `data-engineer-status="deactivated"`, never on the wording. **rev8 adds exactly five constants, all blank with `// Wording owed, NOT approved`, all required by §2.10:** `DEACTIVATED_ON_THIS_PROJECT` (the preview state for a deactivated same-project engineer), `REACTIVATE_CONTROL`, `REACTIVATE_CONFIRM`, `REACTIVATE_RESULT`, `REACTIVATE_ERROR_NOT_FOUND` (mirroring the `DEACTIVATE_*` set). **The unreachability report adds none** — its only text is a developer-facing Sentry title, not user copy. **`DEACTIVATE_CONFIRM` (rev8 note):** it no longer needs to state that deactivation is irreversible, because reactivate ships (§2.10); the plan never asserted irreversibility (grep: none), and what remains true for the wording author is that deactivation **does not free the number** (§2.9). Whether it must name the engineer is still UNKNOWNS #11.

**Formatter (slice 2):** `formatDeactivatedLine(deactivatedByName, deactivatedAt)` (the other formatters are in slice 1 §9).
**Constants (blank, slice 2):** `DEACTIVATE_CONTROL`, `DEACTIVATE_CONFIRM`, `DEACTIVATE_RESULT`, `DEACTIVATE_ERROR_NOT_FOUND`, `ENGINEER_STATUS_DEACTIVATED`, **`DEACTIVATED_ON_THIS_PROJECT`, `REACTIVATE_CONTROL`, `REACTIVATE_CONFIRM`, `REACTIVATE_RESULT`, `REACTIVATE_ERROR_NOT_FOUND` (rev8)**. *(The add-screen constants, `ENGINEER_STATUS_ACTIVE` and `ENGINEERS_LIST_TITLE` are in slice 1 §9.)* **T12** asserts every export is non-empty (expected-fail at commit).

## 10. Pre-flight result — slice 2 rows (test-db `exfccwlrhoutkgrlikod`; the `pg_*` probes are printed in `add-engineer-plan-rev7.txt`)

The log prints `CONFIRMED: project ref reads exfccwlrhoutkgrlikod (test-db)`. All read-only.
| Probe | Result |
|---|---|
| (d) `users.status` | `text`, default `'active'::text`, NOT NULL |
| **pg_1** views mentioning `project_members` (rev7) | **0** |
| **pg_2** public functions whose body mentions it | `correct_daily_log`, `sweep_stale_morning_sessions` (both SECURITY DEFINER; neither returns a roster to a screen) |
| **pg_3** `users` role × status | 10 admin, 1,872 engineer, 3 NULL-role — **all `active`**; no `pending`, no `deactivated` |
| **pg_4** `users` CHECKs | `users_status_check` = `('pending','active','deactivated')`; `users_role_check` = the six roles; `users_notification_email_check` |
| **pg_5** D9/D10 columns on `users` | **none exist** (0 rows) |
| **pg_6** `SELECT` on `users` | table-level for `authenticated` and `anon`; 17 of 17 columns |
| **pg_7** `SELECT … deactivated_at` (expected error; **historical** — demonstrates the mechanism a missing column/table causes; rev9's dependency is the `engineer_episodes` table, §4.9) | `42703: column "deactivated_at" does not exist` |

## 11. For Aravind — run against PROD (not run by me) — slice 2 queries

Read-only; confirm the project ref first. *(Queries `g`, `n`, `n2`, `r`, `u`, `v` are in slice 1; `w` is in both.)*
```sql
-- (w) engineers on prod with NO project membership (the F3 dead-end population)
SELECT count(*) AS orphan_engineers FROM users u
WHERE u.role = 'engineer' AND NOT EXISTS (SELECT 1 FROM project_members pm WHERE pm.user_id = u.id);

-- (v2) legacy deactivated engineers on prod: after 048 they have NO episode, so the board keeps showing them as today and a later reactivation materialises a closed 'backfill' episode (§2.8);
--      also shows whether any 'pending' users exist (the rule must not touch them)
SELECT u.role, u.status, count(*) AS n FROM users u WHERE u.status <> 'active' GROUP BY 1, 2 ORDER BY 1, 2;

-- (n3) engineers with MORE THAN ONE engineer membership on prod — the real, defended state; why the episodes backfill is lazy, never an eager INSERT…SELECT (§2.8)
SELECT user_id, count(*) AS engineer_memberships FROM project_members WHERE role = 'engineer' GROUP BY user_id HAVING count(*) > 1;

-- (u2) AFTER/BEFORE 048: is the table name free on prod?
SELECT to_regclass('public.engineer_episodes') AS engineer_episodes_exists;

-- (e1) AFTER 048 only — invariant I1 drift: engineers whose status and open episodes disagree (§2.8)
SELECT u.id, u.status, count(e.id) FILTER (WHERE e.closed_at IS NULL) AS open_episodes, count(e.id) AS episodes
FROM users u JOIN engineer_episodes e ON e.user_id = u.id
WHERE u.role = 'engineer'
GROUP BY u.id, u.status
HAVING (u.status = 'active' AND count(e.id) FILTER (WHERE e.closed_at IS NULL) <> 1)
    OR (u.status = 'deactivated' AND count(e.id) FILTER (WHERE e.closed_at IS NULL) <> 0);

-- (y) in-flight sessions belonging to deactivated users on prod (F1/F2 population)
SELECT s.current_flow, count(*) AS n FROM whatsapp_sessions s JOIN users u ON u.id = s.user_id
WHERE u.status = 'deactivated' GROUP BY 1 ORDER BY 1;
```

## 12. File list (slice 2)

**Created (slice 2):** `lib/engineers/deactivate.ts` (the other lifecycle files are named below).
**Modified (build slice, slice 2):** `app/(dashboard)/projects/[id]/page.tsx` — **plus (D13, §4.9) `status` in its `users` select and the marker row**.
**D13 (settled, rev7) — modified:** `lib/daily-logs/query.ts` (**in scope by Aravind's scope change, for D13 only**: `getDailyLogsBoard`'s select and roster loop, the `EngineerCard` type, and the new pure `isAfterDeactivation` and `collectMissingCheckins`); `app/(dashboard)/dashboard/page.tsx` (loop → call); `app/(dashboard)/daily-logs/engineer-card.tsx` (marker). **D13 — created:** `app/(dashboard)/projects/[id]/team-member-row.tsx`; tests T23–T29 (`test/deactivation-board.test.ts`, `test/unit/is-after-deactivation.test.ts`, `test/unit/collect-missing-checkins.test.ts`, the two render tests — file names **ASSUMED**). ~~"possibly `lib/daily-logs/status.ts`"~~ — not needed: `deriveHalfStatus` is unchanged.
**rev9 — the `engineer_episodes` table (§2.8) is created by `048`; `lib/daily-logs/query.ts` also reads it (§4.9); `users.deactivated_*`/`reactivated_*` are ~~created~~ not created. No back-dating test helper is specified (D20). ~~The plan split (§13) creates files under `docs/plans/add-engineer/` — a separate commit.~~ (rev10: the split is done — the two files are `add-engineer-plan.md` and `engineer-lifecycle-plan.md`.)** **rev8 — created:** `supabase` side: `reactivate_engineer` in `048` (held in `docs/reviews/` until applied); `lib/engineers/reactivate.ts`; `lib/engineers/unreachable-report.ts` and `app/api/cron/engineer-reachability-report/route.ts` (names **ASSUMED**); `test/` files for T30–T39 (names **ASSUMED**). **rev8 — modified (slice 2):** `lib/daily-logs/status.ts` (Edge 1 branch — **in scope by Aravind's instruction, half-status derivation**), `lib/daily-logs/query.ts` (`getDailyLogDetail`, in addition to §4.9), `components/daily-logs/log-detail-view.tsx`, `app/(dashboard)/daily-logs/page.tsx`, **`vercel.json`** (one cron entry — **merging it is a deploy**), `docs/reviews/048-review-package.md` is shared with slice 1. **Not touched by this plan or the slice:** anything under `app/api/whatsapp/`, `lib/whatsapp/`, `lib/auth/`, or any existing migration; **under `lib/daily-logs/`, only `query.ts` and `status.ts`** (D13); `is-project-pm.ts` and `normalise.ts` are imported, not edited. ~~**This revision's diff:** exactly `docs/plans/add-engineer-plan.md`.~~ (rev10: the split's diff is exactly `docs/plans/add-engineer-plan.md` and `docs/plans/engineer-lifecycle-plan.md`.)

## 13. Plan split — DONE (rev10)

~~**REQUIRED BEFORE THE BUILD PACKAGE — split this plan (rev9; NOT done in this pass)**~~ **Done in rev10** — but not into the seven files rev9 proposed: **Aravind decided the slice itself is too large to review or ship as one piece, so the plan was split into two by content**: `docs/plans/add-engineer-plan.md` (slice 1) and this document (slice 2). See the header of each for what it contains, what it does not, and the dependency. The `file-size-lint` WARN for the single plan no longer applies; each document is reported at the commit. Every decision, finding, test and UNKNOWN is mapped to its destination in the rev10 log (`add-engineer-plan-rev10.txt`).

## Decisions (slice 2)

| | Status |
|---|---|
| ~~**D10**~~ record who deactivated and when, on `users` | **REVERSED (rev9, Aravind):** `deactivated_by/at` are not created; episodes record it (§2.8). External review had signed off on the five-column version — **the design changed after that sign-off.** |
| **D13** F4: deactivated engineers on Daily Logs, Today and the team list | **settled (rev7, Aravind): in this slice, dated by **episodes** (IST; ~~`deactivated_at`~~), NOT a status filter; history unchanged; project detail keeps the row with a marker (§4.9).** ~~OPEN — Option 1/2/3/4~~ |
| **D14** two collisions between D13's rule and "history stays": (i) day D itself with an existing log; (ii) "Needed tomorrow" items from their logs | **flagged default applied, not silently chosen (§4.9):** (i) keep the card, marker, no expectation; (ii) stays shown and counted. One-line alternatives named. For Aravind to overrule; not blocking. **(iii) (rev8):** the Edge 1 branch also covers a morning half on a kept card. |
| **D15** reactivation vs "past dates stay unchanged" | **SETTLED (rev9, Aravind): an `engineer_episodes` table, not `reactivated_by/at`** (§2.8). Reason recorded: the board's question is an interval question; columns answer only the latest interval. Consequences: D10 reversed; the one-episode limit is gone; the reactivation-day morning chip stays named/not built (**D15 (ii)**). |
| **D17** (rev8) unreachability threshold **N** | **recommend N = 3 days**; **decision for Aravind** (§2.11). |
| **D18** (rev9) for an engineer WITH episodes, dates before the first episode are not expected | **ACCEPTED (Aravind, rev10).** An engineer added today no longer reads amber on yesterday's board (§4.9). **The inconsistency, named:** under **lazy materialisation** (§2.8), engineers WITH episodes get the join-side rule and engineers WITHOUT episodes do not, so **the board behaves two ways** — and it stays that way until every engineer has an episode, which lazy materialisation only does on first deactivate/reactivate (**no eager backfill is planned**, and engineers added by slice 1 have none). ~~flagged default; alternative: treat pre-first-episode dates as expected~~ |
| **D19** (rev9) episode FK referential actions and lazy vs eager materialisation | **ACCEPTED as review-package defaults (Aravind, rev10):** `CASCADE` on user/project/tenant (parity with `project_members`), `SET NULL (col)` on `opened_by`/`closed_by`; **lazy** materialisation because an eager backfill would abort on a two-membership engineer (§2.8). The review package may still revisit them; the plan no longer treats them as open. |
| **D20** (rev9 → rev10) gap semantics and the board query | **CHANGED (Aravind, rev10): gap semantics proven in a pure test do NOT prove the board query** — a pure test cannot go red for a query bug. **Specified: one database-level test, T43 (§4.9), asserting that a gap date is excluded from the board, shown failing first.** ~~rev9: not specified; the gap semantics are proven in the pure T23 instead~~ — struck. Mechanism flagged in §4.9 (UNKNOWNS #50). |

## UNKNOWNS (slice 2)

**Not determinable from printed source or the test database:**
1. **Prod state.** Queries n/n2 (roles; CHECK violations); prod phone shapes (Aravind reports one row, `+91`, n=1 — not in this log, unverified by me); users by role; orphans (w); deactivated-user sessions (y); whether prod already has the new columns (u).
3. **Prod/test-db parity for 048's target.** Probes ran on test-db only.
5. **F1's one unproven premise (F1 itself is CLOSED, §2.9):** whether any path can put a **recorded** attendance beside a step-5 session on the same log day. I found no path (writers grep `a1`; `033:61-67`) but did not prove none exists — and **if one existed, the loss would be silent** (the sweep writes no audit row). Nothing else about F1 is open.
8. **Whether the existing `getDailyLogsBoard` tests survive the D13 change.** Read: `dash-03-board.test.ts` (first 60 lines) and `dash-03-board-per-date-text.test.ts` (`:120-180`) — active engineers, no `EngineerCard` literals. **Not read:** `dash-03-board-photo-gate.test.ts` beyond `:67-72`, the two `needed-tomorrow-*` files. T29 is the check; it has not been run.
9. **Apply-time owner.** That the apply role yields `postgres`, as all 15 existing functions.
11. **Whether `DEACTIVATE_CONFIRM` must name the engineer** (would become a formatter).
14. **Attribution design details** (composite FKs, `ON DELETE`, pairing CHECKs, visibility) — recommendations, not rehearsed.
21. **Whether any component I did NOT open could list engineers from `project_members`.** Ruled out by evidence: no `project_members` string under `components/`; no non-literal `.from(` table; no view mentions it (probe pg_1); the two functions that do (probe pg_2) return no roster. **Not ruled out:** (a) the four `components/` subfolders were listed and grepped, **not read**; (b) probes ran on **test-db only** — a prod-only view or function is unseen; (c) a query built outside `app`/`lib`/`components` (e.g. `scripts/`, `supabase/functions`) was not searched for UI reach — none is imported by a page as far as I printed, but I did not trace every import; (d) the PM's actual reaction.
22. **Legacy deactivated rows** (rev9: after 048 they have **no episode**, so the board keeps showing them as today, §2.8): none on test-db (probe pg_3); **prod count unknown** — query `(v2)`.
23. ~~**Whether a past-date DPR regeneration path exists** (F5)~~ **CLOSED (rev8, §2.9 F5):** the by-hand path (`generate-one-dpr.ts`) exists and bypasses the defective code; no repo enqueuer supplies a past date to the job handler. **What remains:** whether an operator has ever inserted a past-date `dpr_generate` job by hand — the `jobs` table is service-role writable and I did not look at prod.
24. ~~**Reactivation** (rev8: one-episode limit; second cycle overwrites the first)~~ — **that limit is gone** (episodes, §2.8). **Open:** (a) whether reactivate should require a **fresh consent attestation** (D9 analogue) — none specified, only who/when is recorded on the episode; (b) the reactivation-day morning chip (D15 ii).
25. **D14 (i)/(ii)** are my resolution of two collisions in the rule as given — for Aravind to confirm or overrule.
26. **Type and shape assumptions in §4.9, unexecuted:** that `HalfStatus['variant']` is assignable to the page's `StatusVariant` (the current loop already assigns it, `dashboard/page.tsx:158,169`); that the PM's RLS-bounded reads return `users.status` and `engineer_episodes` rows (`users_select`, probe j; the new tenant-wide select policy — not run as a PM); the new file names; the marker's `data-engineer-status` attribute (my choice, forced by blank strings).
27. **The board tests were not run, and no query change was executed** — this revision is a plan.
29. **The "has never sent us anything" predicate (§2.11).** Best available source: a `whatsapp_sessions` row for the number (inbound-only writers, printed). **Not established:** whether a bare first message creates a session — the flag-gated branch at `webhook/route.ts:290-300` was not resolved; so **FP-1** (static-ack-only inbound stays flagged) and **FN-1** (a replying stranger hides a typo) are recorded classes, and **FN-2** (fragile if a cron ever starts a session) is guarded by T37 (iv).
30. **The report's query cost** — no index proposed, none measured.
31. **N (D17)** is a recommendation, not data; no observed reply-latency distribution was consulted.
32. **`reactivate_engineer` and in-flight sessions.** A parked evening or hindrance session (F2) becomes processable again on the next inbound; BOT-07's next-day wipe (`012:115-121`) should reset a stale one — read, not run.
33. **Which database Preview deployments read — UNDETERMINABLE from the repo** (evidence and consequence in §4.9). **Aravind must check in the Vercel dashboard:** Preview-scope `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` — read only the host of the URL (`exfccwlrhoutkgrlikod` test-db / `jvxwqignooseazzmwhvl` prod), never paste a key — and whether Preview deployments are enabled for non-`main` branches.
34. **Vercel "promote previous deployment"** as an app rollback — dashboard feature, not read, not relied on.
35. **Prod state, still unread:** query `(v2)`; whether any prod engineer would trip the report on day one (only those with `registered_at`, so none before 048).
37. **No database query was run in rev8 or rev9**; every database statement cites the rev7 probes. Nothing about `reactivate_engineer`, the classifier, the report or the episodes table was executed.
38. **Episode referential actions (D19):** `CASCADE` on `user_id`/`project_id`/`tenant_id` and `SET NULL (opened_by)`/`(closed_by)` — the column-list `SET NULL` form is PG15+ (prod and test-db are 17.6, `CLAUDE.md` §7) and was **not run**; the alternative for the actors is RESTRICT. **rev10: D19 is ACCEPTED as review-package defaults (Aravind).**
39. **Non-overlap of closed episodes is not DB-enforced.** An `EXCLUDE USING gist` needs `btree_gist`; whether the extension is available on prod/test-db was not checked.
40. **Prod engineers with two `engineer` memberships (query `n3`) — unread.** It decides nothing now (lazy materialisation) but would abort an eager backfill.
41. **The marker's meaning on a kept card is "not expected on this date"** while its only wording is `ENGINEER_STATUS_DEACTIVATED`; a pre-first-episode date with a log is unreachable in real data (a log needs a registered engineer) — asserted, not proven.
42. **D18 is a behaviour change to the board for engineers WITH episodes** (pre-join dates stop reading amber); legacy engineers are unchanged. For Aravind to confirm. **rev10: D18 is ACCEPTED (Aravind); the two-way board behaviour under lazy materialisation is named in D18 and §4.9.**
43. **Whether the episodes read fits the board's RLS cost:** the new policy calls `get_user_tenant_id()` per row like its siblings; no plan was measured.
44. ~~**The plan split (§13) is not done** and `file-size-lint` will keep printing the WARN until it is.~~ **DONE in rev10** — the plan is now two documents (§13); this item is closed.
**Assumed:** migration number 048 still free and the file name; routes `projects/[id]/engineers[/new]`; TypeScript gate and SQL function stay in agreement (T6 tests, does not prove); `supabase-js` `rpc` distinguishes `no_data_found` from `insufficient_privilege`; T15 un-testable here; no in-flight bot session depends on the new functions; colliding CI runs on the boundary literal are tolerable; the default `ON DELETE` on attribution FKs is right.
50. **T43's seeding mechanism (§4.9, D20).** A gap date needs episodes with **explicit past timestamps**, which the real functions cannot write (they use `now()`) and `service_role` cannot insert (it holds `SELECT` only). **ASSUMED mechanism:** a **test-db-only `INSERT`/`UPDATE`/`DELETE` grant** on `engineer_episodes` to `service_role`, in the manner of `scripts/test-db-only-grants.sql` (the `outbound_sends` precedent). **`CLAUDE.md` §0 requires such an exception to be *named* there — a `CLAUDE.md` edit and a script edit, both outside `docs/plans/`, are prerequisites for the build package.** The alternative is a test-only SECURITY DEFINER seeding function (precedents `scripts/test-db-only-047-rights-check.sql`). Not decided here.
51. **Slice 2 changes a live function slice 1 shipped** (`add_engineers_to_project` opens an episode) — a logic change to a reviewed SECURITY DEFINER function; gate (a); its grants survive only if the signature is unchanged (`CLAUDE.md` §0).
52. **Engineers added by slice 1 have no episode** and stay on the "no episodes" path (D18's two-way behaviour) until first deactivate/reactivate or a backfill that is not planned.
53. **The episodes design (D15) has not been externally reviewed in this shape** (external review saw the five-column version).
54. **T35 was dropped by the rev9 rewrite and is restored here** (rev10 correction) — the only test found missing between `1765d21` and `eb8a9c2` besides those rev9 replaced by design (T24c, the rev8 forms of T33/T34).
**Decisions still open:** ~~**D13** (product; not blocking)~~ — settled (rev7). **Flagged for confirmation:** **D14** (i)/(ii)/(iii), **D17**; **D18 and D19 are accepted and D20 is specified (rev10)** (none blocking the plan; ~~D15~~ is settled). *(D16 is slice 1.)*
