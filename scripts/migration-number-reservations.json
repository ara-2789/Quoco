[
  {
    "number": "026",
    "claimedBy": "docs/reviews/026_dpr_generation_stale.sql",
    "note": "generation_claimed_at / stale-run reconciliation for dpr_generate. Unapplied draft, relocated out of supabase/migrations/ 2026-08-21 (never committed on any branch, absent from both prod and test-db ledgers as of that date)."
  },
  {
    "number": "034",
    "claimedBy": "docs/reviews/034_owner_email_delivery.sql",
    "note": "Owner-email delivery schema (notification_email, owner_email_verifications). Renumbered from 030 on 2026-08-31 after a real collision with the already-applied 030_morning_flow_attendance.sql. Held on PR #71, blocked on the trigger-cron workstream landing first. This entry exists so the number stays protected even on a branch (like this one) that doesn't yet carry PR #71's own file."
  },
  {
    "number": "035",
    "claimedBy": "docs/reviews/035_evening_flow_restructuring.sql",
    "note": "Evening flow restructuring (5-question redesign) + morning/evening §42 unmatched-token capture + equipment count/hours-used redesign. Originally drafted against a stale '034' in the scoping plan -- corrected the same day when 034 was taken by the owner-email migration (docs/reviews/034-apply-record.md). This entry was itself added late, after the migration-lint reservation rule (PR #148) was run against this branch and correctly flagged the file as unreserved -- recorded here rather than silently, since a reservation added only after being caught by the tool is worth being honest about."
  },
  {
    "number": "039",
    "claimedBy": "docs/reviews/039_hindrance_acknowledgement.sql",
    "note": "DASH-07 Phase 2 -- hindrances gains acknowledged_at/acknowledged_by/ack_notified_at plus a project-scoped RESTRICTIVE RLS UPDATE policy. 038 is the highest applied migration on both prod and test-db as of 2026-09-08 (confirmed: `ls supabase/migrations/` tops out at 038, and no other reservation in this file or any sibling worktree's supabase/migrations/ claims 039). Held in docs/reviews/ pending Aravind's Stage 1 review (migration file + RLS policy diff) before this enters supabase/migrations/ for real, per this file's own 'migration file enters supabase/migrations/ when applied, not when written' rule."
  },
  {
    "number": "040",
    "claimedBy": "docs/reviews/040_evening_q5_tomorrow_needs.sql",
    "note": "Evening Q5 replaced (tomorrow's extra needs, replacing the old hindrance-shaped question) + evening_schedule_miss_reason retired (new evening_tomorrow_needs column; migration-019 correction whitelist swapped, not widened). Reserved 2026-09-11: confirmed 039_hindrance_acknowledgement.sql is already present in supabase/migrations/ on origin/main (9c34be8) AND in test-db's applied ledger (`supabase migration list` against exfccwlrhoutkgrlikod, local/remote both list 039) -- the prior entry's own note describing 039 as still 'held in docs/reviews/' is stale as of this date, corrected by observation here rather than silently trusted. Also checked every sibling .claude/worktrees/*/supabase/migrations/ and */scripts/migration-number-reservations.json for anything claiming 040 or higher -- none found, highest anywhere is 039. Held in docs/reviews/ pending Aravind's Stage 1 review (this migration file + rehearsal output) before this enters supabase/migrations/ for real."
  },
  {
    "number": "041",
    "claimedBy": "docs/reviews/041_write_dpr_version_first_write_fix.sql",
    "note": "write_dpr_version (029) logic fix: a virgin row's first-ever call currently jumps current_version straight to 2 (current_version + 1, off the column's own untouched DEFAULT 1) instead of landing at 1 -- distinct from 029's own already-fixed B3 phantom-v1 case (a row that already HAD content before its first RPC call), which this does not touch or regress. Argument signature UNCHANGED (UUID, TEXT, JSONB, TEXT, UUID) -- CREATE OR REPLACE only, no DROP FUNCTION, no re-grant. Reserved 2026-09-12: confirmed 040_evening_q5_tomorrow_needs.sql is the highest file present in supabase/migrations/ on origin/main (fetched fresh, not assumed) and the highest reservation in this file. Also checked every sibling .claude/worktrees/*/supabase/migrations/ (highest anywhere: 040) and */scripts/migration-number-reservations.json (highest anywhere: 040) for anything claiming 041 or higher -- none found, across all ~30 active worktrees at reservation time. Held in docs/reviews/ pending rehearsal (forward + DOWN, proving both the virgin-row case AND that an existing row's regeneration still lands at current_version + 1 exactly as it does live today) and Aravind's external review, per §0's gate (this trips condition (a) -- live SECURITY DEFINER function logic change)."
  }
]
