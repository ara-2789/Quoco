[
  {
    "number": "026",
    "claimedBy": "docs/reviews/026_dpr_generation_stale.sql",
    "note": "generation_claimed_at / stale-run reconciliation for dpr_generate. Unapplied draft, relocated out of supabase/migrations/ 2026-08-21 (never committed on any branch, absent from both prod and test-db ledgers as of that date)."
  },
  {
    "number": "034",
    "claimedBy": "docs/reviews/034_owner_email_delivery.sql",
    "note": "Owner-email delivery schema (notification_email, owner_email_verifications). Renumbered from 030 on 2026-08-31 after a real collision with the already-applied 030_morning_flow_attendance.sql. ~~Held on PR #71, blocked on the trigger-cron workstream landing first. This entry exists so the number stays protected even on a branch (like this one) that doesn't yet carry PR #71's own file.~~ DATED CORRECTION (2026-09-13, found stale during the 042 reservations audit): APPLIED. 034_owner_email_delivery.sql is present in supabase/migrations/ on main; see docs/reviews/034-apply-record.md for the apply sequence. This entry's own 'held, blocked' language was never updated after that apply -- caught only while auditing every entry in this file for the same staleness pattern 039's own entry already showed, not independently noticed before."
  },
  {
    "number": "035",
    "claimedBy": "docs/reviews/035_evening_flow_restructuring.sql",
    "note": "Evening flow restructuring (5-question redesign) + morning/evening §42 unmatched-token capture + equipment count/hours-used redesign. Originally drafted against a stale '034' in the scoping plan -- corrected the same day when 034 was taken by the owner-email migration (docs/reviews/034-apply-record.md). This entry was itself added late, after the migration-lint reservation rule (PR #148) was run against this branch and correctly flagged the file as unreserved -- recorded here rather than silently, since a reservation added only after being caught by the tool is worth being honest about."
  },
  {
    "number": "039",
    "claimedBy": "docs/reviews/039_hindrance_acknowledgement.sql",
    "note": "DASH-07 Phase 2 -- hindrances gains acknowledged_at/acknowledged_by/ack_notified_at plus a project-scoped RESTRICTIVE RLS UPDATE policy. 038 is the highest applied migration on both prod and test-db as of 2026-09-08 (confirmed: `ls supabase/migrations/` tops out at 038, and no other reservation in this file or any sibling worktree's supabase/migrations/ claims 039). ~~Held in docs/reviews/ pending Aravind's Stage 1 review (migration file + RLS policy diff) before this enters supabase/migrations/ for real, per this file's own 'migration file enters supabase/migrations/ when applied, not when written' rule.~~ DATED CORRECTION (2026-09-13, found stale during the 042 reservations audit): APPLIED. 039_hindrance_acknowledgement.sql's own header has carried its own 'DATED CORRECTION: APPLIED TO PROD' note since 2026-09-08 (same day as this entry's reservation) and the file has been in supabase/migrations/ since -- this entry was simply never updated to match, for over a month. See docs/reviews/039-apply-record.md for the apply sequence."
  },
  {
    "number": "040",
    "claimedBy": "docs/reviews/040_evening_q5_tomorrow_needs.sql",
    "note": "Evening Q5 replaced (tomorrow's extra needs, replacing the old hindrance-shaped question) + evening_schedule_miss_reason retired (new evening_tomorrow_needs column; migration-019 correction whitelist swapped, not widened). Reserved 2026-09-11: confirmed 039_hindrance_acknowledgement.sql is already present in supabase/migrations/ on origin/main (9c34be8) AND in test-db's applied ledger (`supabase migration list` against exfccwlrhoutkgrlikod, local/remote both list 039) -- the prior entry's own note describing 039 as still 'held in docs/reviews/' is stale as of this date, corrected by observation here rather than silently trusted. Also checked every sibling .claude/worktrees/*/supabase/migrations/ and */scripts/migration-number-reservations.json for anything claiming 040 or higher -- none found, highest anywhere is 039. ~~Held in docs/reviews/ pending Aravind's Stage 1 review (this migration file + rehearsal output) before this enters supabase/migrations/ for real.~~ DATED CORRECTION (2026-09-13, found stale during the 042 reservations audit): APPLIED. 040_evening_q5_tomorrow_needs.sql is present in supabase/migrations/ on main; see docs/reviews/040-apply-record.md for the apply sequence. This entry's own 'held' language was never updated after that apply -- the exact same drop pattern this entry's own text once caught 039's entry doing, now caught here on itself."
  },
  {
    "number": "041",
    "claimedBy": "docs/reviews/041_write_dpr_version_first_write_fix.sql",
    "note": "write_dpr_version (029) logic fix: a virgin row's first-ever call currently jumps current_version straight to 2 (current_version + 1, off the column's own untouched DEFAULT 1) instead of landing at 1 -- distinct from 029's own already-fixed B3 phantom-v1 case (a row that already HAD content before its first RPC call), which this does not touch or regress. Argument signature UNCHANGED (UUID, TEXT, JSONB, TEXT, UUID) -- CREATE OR REPLACE only, no DROP FUNCTION, no re-grant. Reserved 2026-09-12: confirmed 040_evening_q5_tomorrow_needs.sql is the highest file present in supabase/migrations/ on origin/main (fetched fresh, not assumed) and the highest reservation in this file. Also checked every sibling .claude/worktrees/*/supabase/migrations/ (highest anywhere: 040) and */scripts/migration-number-reservations.json (highest anywhere: 040) for anything claiming 041 or higher -- none found, across all ~30 active worktrees at reservation time. Held in docs/reviews/ pending rehearsal (forward + DOWN, proving both the virgin-row case AND that an existing row's regeneration still lands at current_version + 1 exactly as it does live today) and Aravind's external review, per §0's gate (this trips condition (a) -- live SECURITY DEFINER function logic change). DATED CORRECTION (2026-09-13, found stale during the 042 reservations audit): APPLIED. 041_write_dpr_version_first_write_fix.sql is present in supabase/migrations/ on main. NAMED GAP, not filled here: unlike every other applied migration referenced in this file, no docs/reviews/041-apply-record.md exists -- this correction can confirm the file is applied (by its presence in supabase/migrations/) but not paste the apply sequence a proper record would carry. Flagged for whoever next touches 041, not fabricated here."
  },
  {
    "number": "042",
    "claimedBy": "docs/reviews/042_storage_bucket_setup.sql",
    "note": "Stage 0 of the media capability (docs/plans/media-capture-design.md item 20; docs/plans/stage0-storage-setup-plan.md): creates one private Storage bucket ('daily-log-photos') via a plain INSERT INTO storage.buckets -- no table, no RLS policy, no ingestion. Reserved 2026-09-13: confirmed 041_write_dpr_version_first_write_fix.sql is the highest file present in supabase/migrations/ on origin/main (fetched fresh) and the highest reservation in this file -- 042 was free. NOT independently checked against every sibling .claude/worktrees/*/supabase/migrations/ or reservations file this time (named as a limit on this reservation's own confidence, per this file's own established practice of naming what wasn't checked rather than skipping it silently). Held in docs/reviews/ pending a real dry-run/rehearsal against test-db (never attempted this pass -- no Supabase credentials of any kind exist in the environment that wrote this file) and Aravind's review. Does not trip CLAUDE.md §0's external review gate on its own reading (no function logic, no grants/RLS on an existing object, no auth/identity surface, nothing destructive, no money) -- a new bucket with no policies is additive infrastructure, not a change to anything's access rules; flagged for Aravind to confirm this reading, not asserted as final. DATED CORRECTION (2026-09-13): APPLIED. supabase db query --linked -f supabase/migrations/042_storage_bucket_setup.sql, prod ref jvxwqignooseazzmwhvl, no error. Post-apply readback confirmed by observation (id=daily-log-photos, name=daily-log-photos, public=false). Ledger repaired (supabase migration repair --status applied 042 --linked); supabase migration list --linked shows Local and Remote matching through 042. THE OPEN QUESTION IS ANSWERED: the normal CLI apply path does have privilege to write storage.buckets -- no dashboard exception needed, here or for future Storage work. Full sequence: docs/reviews/042-apply-record.md."
  },
  {
    "number": "043",
    "claimedBy": "docs/reviews/043_daily_log_photos.sql",
    "note": "Stage 1 of the media capability (docs/plans/media-capture-design.md item 20; docs/plans/stage1-photo-intake-plan.md): creates daily_log_photos per item 7's per-parent shape, with real Postgres RLS (PM-scoped SELECT, joining through daily_logs to project_members) and an explicit service_role DELETE/TRUNCATE revoke (the table is tombstoned, never hard-deleted -- closing the same class of gap already found twice on this project, dpr_versions/outbound_sends). Reserved 2026-09-13: confirmed 042_storage_bucket_setup.sql is the highest file present in supabase/migrations/ on origin/main and the highest reservation in this file -- 043 was free. NOT independently checked against every sibling worktree's own supabase/migrations/ or reservations file this time -- named as a limit on this reservation's own confidence, per this file's own established practice. Held in docs/reviews/ pending a real dry-run/rehearsal against test-db (not attempted this pass -- no Supabase credentials in the environment that wrote this file) and the full external review package (trips CLAUDE.md's own gate: a new table with its own RLS and grants from day one). DATED CORRECTION (2026-09-14): APPLIED. Two external review rounds preceded the apply (round 1: STOP, fold-and-return, six items; round 2: GO, conditional on the no-app-DELETE invariant test, landed in PR #270 before merge). supabase db query --linked -f supabase/migrations/043_daily_log_photos.sql, prod ref jvxwqignooseazzmwhvl, no error. Post-apply readback confirmed by observation (table_exists=daily_log_photos, status_cols=2, rls_enabled=true, policy_count=1); the generated expires_at column's own expression re-confirmed via pg_get_expr; the four-way negative grants matrix (DELETE/TRUNCATE/REFERENCES/TRIGGER, all false) is prod's own, the sole authoritative record now that test-db's matrix deliberately reads DELETE=true. Ledger repaired (supabase migration repair --status applied 043 --linked); supabase migration list --linked shows Local and Remote matching through 043. THIS IS THE FIRST REAL APPLICATION OF RULE B (reservations updated AT APPLY TIME, not left stale) -- this exact edit was nearly skipped on its first outing; see docs/reviews/043-apply-record.md for that note in full, and for the complete apply sequence."
  },
  {
    "number": "044",
    "claimedBy": "docs/reviews/044_hindrance_photos.sql",
    "note": "Stage 2 of the media capability (docs/plans/media-capture-design.md item 20; docs/plans/stage2-hindrance-photos-plan.md): creates hindrance_photos per item 7's per-parent shape (reusing the daily-log-photos bucket, not a second one -- Aravind's 2026-09-14 decision), adds hindrances.photos_status, and CREATE OR REPLACEs apply_hindrance_flow_turn to add a third (photo) question and return the newly-inserted row's id. Reserved 2026-09-14: confirmed 043_daily_log_photos.sql is the highest file present in supabase/migrations/ on origin/main (fetched fresh, 49e2c8b) and the highest reservation in this file -- 044 was free. NOT independently checked against every sibling worktree's own supabase/migrations/ or reservations file this time (the sandbox refused a cross-worktree glob for this session, being isolated to its own worktree) -- named as a limit on this reservation's own confidence, per this file's own established practice. Trips CLAUDE.md's own external review gate on two independent grounds: a new table with its own RLS/grants from day one, AND a live SECURITY DEFINER function's logic changing (a new step, a new completion point, a new return field). Held in docs/reviews/ pending the full external review package; applied to TEST-DB ONLY this pass (exfccwlrhoutkgrlikod) per Aravind's explicit instruction -- NOT applied to prod. See docs/reviews/044-review-package.md for the apply sequence and evidence. DATED CORRECTION (2026-09-15): APPLIED TO PROD (jvxwqignooseazzmwhvl). External review returned a fold-and-return round first (principal finding S1: resolveMostRecentHindranceId -- the same failure shape as the was_unspecified bug found internally, one layer up -- resolved by carrying hindrance_id through session context and deleting the resolver; folded in, re-verified against test-db, PR #275 went CI-green and was merged). PITR confirmed live in the dashboard before applying (restore window 08 Sep 2026 22:00:34 to 15 Sep 2026 00:03:53 IST, observed, not assumed). Pre-apply function hash abdac08cd997bb2e3d8d73773d807a24 (038's own body, unchanged since). supabase db query --linked -f supabase/migrations/044_hindrance_photos.sql -- no error. Post-apply readback confirmed by observation: table_exists=hindrance_photos, status_col=1, rls_enabled=true, policy_count=1, post_044_hash=09b4e083638dd359b6415a71f6146bec -- the hash pair (abdac08c...->09b4e083...) is the record the RPC rewrite landed. The generated expires_at expression re-confirmed via pg_get_expr. The four-way negative grants matrix is PROD's own, the sole authoritative record now that test-db's own matrix deliberately reads service_role DELETE=true (scripts/test-db-only-grants.sql). Ledger repaired (supabase migration repair --status applied 044 --linked succeeded, \"Repaired migration history: [044] => applied\"); supabase migration list --linked shows Local and Remote matching through 044, no gaps. Types regenerated (54 insertions to types/database.ts, confirmed by grep to contain hindrance_photos, both its foreign keys, and hindrances.photos_status) -- run while linked to TEST-DB, not prod, both carry 044 and generated types exclude grants, so the output is equivalent. Full apply sequence, the CI-never-actually-ran incident, the real-send size gate, and the still-owed first-real-report closing artifact: docs/reviews/044-apply-record.md."
  },
  {
    "number": "045",
    "claimedBy": "docs/reviews/045_media_nudge_throttle.sql",
    "note": "Stage 3 of the media capability (docs/plans/media-capture-design.md item 20): a new claim_media_nudge(text, uuid, uuid, timestamptz, integer) function throttling the idle-photo nudge to one reply per 5-minute window per phone number, using the same INSERT ... ON CONFLICT (phone_number) DO UPDATE row-lock acquire pattern as acquire_and_transition_session (012) and apply_hindrance_flow_turn (044); writes exactly one context key, never touches updated_at/expires_at/current_flow/current_step. Reserved 2026-09-15: confirmed 044_hindrance_photos.sql is the highest file present in supabase/migrations/ on origin/main (fetched fresh, fbced4b) and the highest reservation anywhere -- checked this worktree's own supabase/migrations/ and scripts/migration-number-reservations.json, plus every sibling .claude/worktrees/*/supabase/migrations/ and */scripts/migration-number-reservations.json (highest anywhere: 044) -- 045 was free. Held in docs/reviews/ pending a real dry-run/rehearsal against a local disposable Postgres scaffold (CLAUDE.md §7) and Aravind's external review -- this pass's own explicit instruction is not to apply to prod OR any remote database (test-db included) at all. Trips CLAUDE.md's own external review gate on condition (a): a brand-new SECURITY DEFINER function has no prior safe state to fall back on, per the gate's own wording, even though nothing existing is being modified. See docs/reviews/045-review-brief.md for the full review package."
  }
]
